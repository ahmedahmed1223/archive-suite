# مهام الإصدار الأول — Archive Suite

> هذا الملف هو لوحة العمل الحالية فقط. التفاصيل المثبتة للمنجزات تُنقل إلى
> [`ChangeLog.md`](ChangeLog.md). لا تُغلق مهمة إلا بدليل تحقق مناسب، ولا يُخفي
> بند محجوب متطلبًا خارجيًا.

## الحالة التنفيذية

**المسار القانوني للتطوير:** جميع المزايا الجديدة تُنفذ في `archive-next` و`archive-laravel`، ويظل عقد API في `docs/api` مصدر الحقيقة؛ لا تُضاف مميزات جديدة إلى المسارات القديمة.

| الحالة | العدد | المعنى |
|---|---:|---|
| قابلة للتنفيذ محليًا | 0 | لا تحتاج اعتمادًا أو بيئة خارجية جديدة |
| محجوبة خارجيًا | 12 | تحتاج حسابًا أو جهازًا نظيفًا أو عينة بشرية أو بيئة قبول |
| مرجعية مستوردة | 18 | خريطة CloudMediaDB؛ لا تُغلق قبل نطاق تنفيذ مستقل |
| **المجموع المفتوح** | **30** | مطابق لعدد بنود `- [ ]` في هذا الملف |

> العدّ أعلاه يحسب بنود القائمة لا الرموز المفردة؛ ومن البنود المحجوبة نطاقات
> (`V1-806–V1-814`، `V1-501–V1-505`، `V1-601–V1-605`) تمثل 24 رمزًا. للتحقق:
> `grep -c '^- \[ \]' TASKS.md`.

## إصلاحات بنية تحتية

- [x] **إصلاح جذري: فشل CI المتكرر في بوابة Playwright الحية** — ثلاثة تشغيلات
  CI متتالية (2026-07-29/30) فشلت بنفس النمط بالضبط: 3 اختبارات تفشل دومًا
  (`onboarding-progress`, `scheduled-uploads` × 2) بخطأ
  `Target page, context or browser has been closed` بعد "element was detached
  from the DOM, retrying" على أول `fill()` لحقل البريد الإلكتروني. السبب
  الجذري: هذه الملفات تستخدم `roleSession()` (يسجّل الدخول عبر API مباشرة
  ويضع كوكي `va_session` صالحة) ثم تتنقل يدويًا إلى `/login` وتملأ نموذج الدخول
  مرة أخرى — ما يتسابق مع `useEffect` في `app/login/page.tsx` الذي يُعيد
  التوجيه فورًا بعيدًا عن `/login` بمجرد اكتشاف جلسة موثّقة بالفعل. على جهاز
  التطوير المحلي كسب `fill()` هذا السباق غالبًا؛ على عدّاء CI الأبطأ/المشترك
  كسب إعادة التوجيه بثبات، فأزال حقل النموذج من الـDOM أثناء الملء. وُجد نفس
  الخلل بالضبط في ثلاثة ملفات (`onboarding-progress.authed.spec.ts` مرتين
  داخل نفس الاختبار، `scheduled-uploads.authed.spec.ts` مرتين،
  `dropbox-folder-picker.authed.spec.ts` مرة) — لا حالة أخرى فى `e2e/` تطابق
  هذا النمط (تحقق شامل عبر بحث `getByLabel('البريد الإلكتروني')` في كل
  الملفات). الإصلاح: إزالة دالة `login()` المكررة والتنقل مباشرة إلى الصفحة
  الهدف باستخدام الجلسة الموثّقة أصلاً من `roleSession()` — نفس النمط الذي
  تتبعه بقية الاختبارات الناجحة. تحقق حي: تشغيل البوابة الكاملة على الملفات
  الثلاثة المعدَّلة، 5/5 ناجحة (3.4 دقيقة)، ومجموعة الواجهة 531/531 ناجحة
  بلا تراجع.

## الأولوية التالية — قابلة للتنفيذ محليًا

- [x] **V1-762 تكامل Dropbox** — تم بالكامل محليًا. متصفح اختيار مجلد فعلي
  (`browseFolders`/`setFolder` + `DropboxFolderPicker`) واختبار Playwright
  للواجهة (`e2e/dropbox-folder-picker.authed.spec.ts`، 6/6 ناجحة). النقل الكبير
  القابل للاستئناف: `DropboxIngestTransport` (نفس عقد `IngestTransport` مثل
  FTP/SMB) يسحب من المجلد المتصل عبر مسار `POST /ingest/dropbox/pull`، بتنزيلات
  Range مجزأة (`ingest.chunk_upload.max_chunk_bytes`، معاد استخدامه من آلية رفع
  V1-711 بدل مفتاح جديد) وجدول `dropbox_download_progress` يحفظ التقدم لكل
  (اتصال، مسار) — انقطاع منتصف ملف كبير يستأنف من آخر بايت وصل بدل إعادة البدء
  أو التجاوز الصامت. عقد OpenAPI وعميل Next.js المُولَّد محدَّثان
  (`pnpm api:generate` + `verify:api-contracts`/`verify:api-generated` كلاهما
  ناجح). تحقق حي: مجموعة PHPUnit كاملة 850 تحقق ناجح/0 فاشل (تشمل 5 اختبارات
  جديدة للنقل تُثبت التجزئة والاستئناف والتخطي عند الاكتمال)، ومجموعة الواجهة
  524 ناجحة، وبوابة `verify:laravel-next:live` الكاملة 151/151 دون أي تراجع.
  التحقق الحي للاتصال الفعلي بحساب Dropbox يبقى ضمن V1-X01.
- [x] **V1-791 تدقيق العربية** — تم؛ النتائج في `docs/v1-791-arabic-audit.md`.
  الإشعارات عربية بالكامل، واتجاه الأيقونات سليم عدا موضع واحد. المتبقي انتقل
  إلى V1-818.
- [x] **V1-818 تعريب رسائل الأخطاء (الشق الأمامي)** — تم؛ `translateKnownApiError`
  صار يعتمد `code` أولًا مع خريطة عربية لـ38 رمزًا، والرموز غير المعروفة تمر
  كما هي. مواضع المقارنة بالنص الإنجليزي 4 فقط (لا ~50) وكلها تفحص `code` أولًا.
- [x] **V1-823 تعريب رسائل التحقق في Laravel** — تم؛ `APP_LOCALE=ar` افتراضيًا،
  و`lang/ar/validation.php` يغطي القواعد المستخدمة فعليًا فقط (القواعد غير
  المستخدمة تتساقط إلى الإنجليزية عبر `APP_FALLBACK_LOCALE=en`). تحقق حي عبر
  مجموعة PHPUnit كاملة (274+140 ناجحة، 0 فاشلة)، وتصحيح اختبار واحد كان
  يثبّت النص الإنجليزي القديم.
- [x] **V1-795 تدقيق UI/UX للموبايل** — تم؛ التمريرتان نُفِّذتا حيًّا (95 ناجحة،
  56 فاشلة). النتائج في `docs/v1-795-mobile-audit.md` وتحولت إلى V1-819/820/821.
- [x] **V1-819 (P1) روابط قائمة «المزيد» تهرب من `<details>` المغلق** — السبب:
  `.nav-section { display: contents }` يجعلها تُرسم خارج الحافة وتبقى في ترتيب
  التركيز بين 761 و1119. الإصلاح: `.nav-more:not([open]) .nav-section
  { display: none }`. النتيجة الحية: 768 من 0/50 إلى 39/11، و1280 بقي 49/49.
- [x] **V1-822 (P2) تلميح لوحة الأوامر «Ctrl / Cmd + K» خارج الشاشة عند 768** —
  تم مع V1-820 (نفس السبب الجذري)؛ `.topbar-actions` صار `grid-column: 1/-1`
  بدل عمود `auto` مشترك بلا سقف عرض مع `.nav-toggle`. تحقق حي: 768 صار 0/48.
- [x] **V1-820 (P1) زر «المسارات» خارج الشاشة عند 375** — تم؛ نفس إصلاح
  `.topbar-actions` أعلاه. تحقق حي عبر البوابة الكاملة (150 ناجحة/1 فاشلة، من
  134/17)؛ 768 و1280 صارا 100%. الفاشل الوحيد المتبقي في كل المجموعة: `/rights`
  عند 375 (V1-821)، سبب منفصل تمامًا.
- [x] **V1-821 (P2) `/rights` يرسم «فحص الإنفاذ» ثلاث مرات خارج الشاشة** — تم
  (بعد إعادة فتح واحدة، انظر `docs/v1-795-mobile-audit.md`). السبب: عمود
  «الإنفاذ» آخر عمود DOM في جدول RTL يُرسم بصريًا في أقصى اليسار، وحاوية
  `.scroll-x` تستقر افتراضيًا عند بداية القراءة (يمين) فيُقصى العمود
  الإجرائي من أول رسم. الإصلاح: تثبيته (`position: sticky; inset-inline-end:
  0`) عبر صنف اختياري جديد `.data-table-sticky-end` (وليس قاعدة عامة على كل
  `.data-table :last-child` — نصف الجداول الخمسة عشر ينتهي بعمود بيانات عادي
  لا إجراء). طُبِّق أولًا على `/rights` فقط؛ تحقق حي: البوابة الكاملة 151
  ناجحة/0 فاشلة — كل مسار وكل عرض شاشة. ثم طُبِّق الصنف نفسه على بقية الجداول
  ذات عمود الإجراء الأخير (backup، delegations [عرض "granted" فقط]، favorites،
  shares، trash) — تحقق عبر typecheck ومجموعة الواجهة الكاملة (524 ناجحة، بلا
  إعادة تشغيل للبوابة الحية لأن الآلية نفسها مُتحقق منها حيًّا مسبقًا.
- [x] **V1-846 توحيد أسماء الملفات** — تم؛ `lib/filename-normalization.ts`
  (`suggestFilename`) يقترح [نوع]-[تاريخ]-[عنوان مبسّط][الامتداد الأصلي] من
  بيانات السجل الموجودة أصلًا، بلا إعادة تسمية تلقائية — اقتراح فقط. تحقق:
  5/5 اختبارات جديدة ناجحة.
- [x] **V1-852 حافظة البيانات الوصفية** — تم؛ `lib/metadata-clipboard.ts` حافظة
  محلية بخانة واحدة (نسخ/لصق أثناء الجلسة) للوصف/النوع/الوسوم فقط — العنوان
  مستبعد عمدًا لأنه يميّز السجل. `applyClipboard` يطبّق الحقول المطلوبة فقط.
  تحقق: 5/5 اختبارات جديدة ناجحة.
- [x] **V1-863 إظهار الحقول حسب نوع المادة** — تم؛ `lib/type-field-visibility.ts`
  يشتق حقول النوع من `ArchiveType.fields` الموجود أصلًا (`/types`)، و
  `mergeVisibleFieldValues` يحدّث حقول النوع الظاهر فقط ويترك أي بيانات وصفية
  تابعة لنوع سابق كما هي — الحفاظ على البيانات المخفية. تحقق: 3/3 ناجحة.
- [x] **V1-844 تصدير حزمة بيانات المادة** — تم؛ `lib/record-export.ts` يبني
  حزمة نصية قابلة للقراءة (العنوان، الوصف، النوع، الوسوم، الحقوق، سجل النشاط)
  من بيانات صفحة المادة المحمّلة أصلًا — بلا ملفات أو رموز أو روابط مشاركة.
  تحقق: 3/3 ناجحة.
- [x] **V1-864 تقرير تسليم داخلي** — تم؛ `lib/handoff-report.ts` يبني ملخصًا
  قابلًا للطباعة عبر مجموعة مواد (الحالة عبر `deriveRecordStatus` من V1-851،
  الحقوق، عدد التعليقات) بلا نظام تقارير جديد. تحقق: 4/4 ناجحة.
- [x] **V1-858 قواعد تسمية حسب المشروع** — تم بتخزين قاعدي (`naming_rules`
  جدول Laravel، لا localStorage — بيانات مشتركة بين المستخدمين). مسار عبر
  `NamingRulesController` (index/upsert/destroy) وعقد OpenAPI جديد
  (`NamingRule`/`NamingRulesResponse`)، وعميل Next.js
  (`client.namingRules`/`upsertNamingRule`/`deleteNamingRule`) ودالة تحقق
  نقية `lib/naming-rules-check.ts` (`checkFilename`) لا تُعيد تسمية شيء —
  اكتشاف واقتراح فقط. تحقق: 4/4 PHPUnit ناجحة، 3/3 vitest ناجحة،
  `verify:api-contracts` ناجح.
- [x] **V1-861 ربط المادة بمشروع** — تم بتخزين قاعدي (`projects` +
  `project_records` pivot). `ProjectsController` (إنشاء/حذف مشروع، ربط/فك ربط
  سجل بلا نقله من نوعه أو مجلده، قائمة مواد المشروع، قائمة مشاريع المادة)،
  عقد OpenAPI وعميل Next.js مطابقان. صفحة المشروع الخفيفة نفسها لم تُبنَ بعد —
  هذا البند يغطي الطبقة الخلفية والعميل فقط. تحقق: 4/4 PHPUnit ناجحة،
  `verify:api-contracts` ناجح.
- [x] **V1-840 تقسيم المادة إلى مقاطع** — تم بتخزين قاعدي (`record_segments`
  جدول Laravel). `RecordSegmentsController` (CRUD كامل: عنوان، وصف، وسوم،
  بداية/نهاية بالثواني) بلا نسخ ملف — مقاطع مرتبطة بمعرّف المادة فقط. تحقق:
  4/4 PHPUnit ناجحة (تشمل اختبار عزل المقاطع بين السجلات)، `verify:api-contracts` ناجح.
- [x] **V1-833 مركز تنظيف الأرشيف** — تم كطبقة تجميع نقية؛ `lib/cleanup-center.ts`
  (`buildCleanupItems`) يصنّف بيانات محمّلة أصلًا (سجلات، ملفات، رفعات مجدولة)
  إلى أربع فئات: ناقصة (عبر `deriveRecordStatus`)، ملف يتيم (بلا مسار سجل
  مطابق)، رفع فاشل، تكرار محتمل (نفس الاسم والحجم) — بلا محرك تحليلات جديد
  وبلا استدعاء API إضافي؛ المستدعي يزوّد البيانات من الاستدعاءات القائمة.
  صفحة العرض نفسها لم تُبنَ بعد. تحقق: 5/5 اختبارات جديدة ناجحة.
- [x] **V1-859 إشعار سعة التخزين** — تم؛ `lib/storage-capacity-alert.ts`
  (`checkStorageCapacity`) عتبتان: 80% تحذير، 95% حرج، مبنيتان على قياس
  `usedBytes`/`totalBytes` الذي يزوّده المستدعي من حالة النظام القائمة — بلا
  مراقبة سحابية جديدة. تحقق: 4/4 اختبارات جديدة ناجحة.
- [x] **V1-786 فصل تنفيذ الوسائط** — عقد versioned لـ`media_jobs` وعامل قابل
  للتوسع مع بقاء Laravel مالك الجدولة والحالة والتدقيق. التحقق 2026-07-31:
  `MediaJobsApiTest` و`MediaJobsReliabilityTest` (28 اختبارًا) وعقد OpenAPI
  والعميل المولّد متطابقان.
- [x] **V1-790 طبقة وصول موحدة للأرشيف** — ترحيل تدريجي لمسارات المجال إلى
  `storage_rows` بهوية `[store, uid]`. التحقق 2026-07-31: مسارات السجلات
  والبحث والأنواع وإنهاء الرفع تستخدم المستودع؛ اختبار مصفوفة السجلات/السلة
  والأنواع والمستودع 45 اختبارًا ناجحًا.
- [ ] **V1-303D الوصولية** — لوحة مفاتيح وقارئ شاشة وعينات عملية للمسارات
  اليومية. **محجوب قدرة:** أدلة Playwright الحية موجودة، لكن وثيقة الإغلاق
  تشترط عينة بشرية على NVDA أو VoiceOver؛ لا يتوفر NVDA محليًا وWindows Narrator
  لا يطابق معيار الدليل المحدد.
- [x] **V1-306B العربية وRTL** — تم بكامل شقيه. الشق الأول: مسح ثابت لكل نصوص
  JSX الإنجليزية الحرفية عبر `app/` و`components/` وجد موضعين وأصلحهما («Rundown»
  في `app/broadcast/page.tsx` صار «الراندون»، و«Metadata» في `app/uploads/page.tsx`
  صارت «بيانات وصفية»). الشق الثاني (المتبقي سابقًا): جلسة تدقيق حية لجودة
  الصياغة العربية — قراءة كاملة لـ32 صفحة من المسارات الأقل استخدامًا
  (graph، kanban، map، vocabulary، plugins، data-center، automation،
  collaboration، duplicates، transcriber، safety-preview، system/control،
  reading-lists، analytics، daily، collections، discover، errors، copilot،
  sync، status، media/*، timeline، inbox، notifications، projects، tags،
  types، uploads/scheduled، settings/users) وجدت 5 عيوب حقيقية: خطأ إملائي
  متكرر (سقوط ألف التنوين من «حالياً») في 3 ملفات (`collaboration`، `discover`،
  `errors`)، الخطأ نفسه في «قراراً» (`sync`)، وترجمة خاطئة في `notifications`:
  زر/aria-label «وضّح» (فعل أمر بمعنى "وضّح الأمر" وليس "حدّد") صار «تحديد»
  ليطابق فعل التحديد الفعلي (mark as read). لم يُرصد أي تعارض مصطلحات بين
  الملفات. تحقق: `tsc --noEmit` نظيف، ومجموعة الواجهة 524/524 ناجحة بلا تراجع.
- [x] **V1-824 مؤشر جاهزية المادة** — تم؛ لوحة `RecordReadinessPanel` في
  `app/archive/[id]/page.tsx` مشتقة بالكامل من بيانات السجل المحمّلة أصلاً
  (لا استدعاء API جديد): الملف عبر `deriveRecordSourcePath`، العنوان/الوصف/
  الوسوم من السجل نفسه، الحقوق من `state.rights`، والمراجعة كمقياس تقريبي
  (وجود تعليق فريق واحد على الأقل، إذ لا يوجد حقل حالة مراجعة مخصص). تعرض
  عدد العناصر المكتملة والإجراء التالي المقترح لأول عنصر ناقص، من دون منع
  الحفظ أو فرض دورة اعتماد. تحقق: `tsc --noEmit` نظيف، ومجموعة الواجهة
  527/527 ناجحة (تشمل 3 اختبارات جديدة لمنطق `buildReadinessItems`).
- [x] **V1-825 قوائم العمل الشخصية المحفوظة** — تم؛ `lib/work-lists.ts` يعرّف
  أربع قوائم عمل فوق الفلاتر القائمة لا فوق خدمة مهام أو تخزين جديد: المواد
  ناقصة التوصيف (`/archive?completion=incomplete`)، المسودات (`?status=draft`)،
  ما ينتظر المراجعة (`?status=review`)، والحقوق التي تقترب من الانتهاء
  (`/rights`). القيد الوحيد المضاف فعليًا هو `completion=incomplete` في
  `/archive`: يشتق من `descriptorCompletion` الذي يرسله الخادم أصلاً (لا استدعاء
  API جديد)، وله شريحة ظاهرة قابلة للإلغاء ضمن صف الفلاتر حتى لا يصير فلترًا
  خفيًا، ومسجَّل في الرابط وفي عدّاد الفلاتر النشطة و«تصفير». حفظ القائمة على
  مستوى المستخدم يستخدم آلية «العروض المحفوظة» الموجودة مسبقًا بدل كيان جديد.
  عرضت لوحة «قوائم العمل» في `/daily` مع عدّاد الحقوق فقط — نقطة `records`
  مقسّمة بمؤشر بلا إجمالي، فعدّ قوائم الأرشيف كان سيتطلب المرور على كل الصفحات؛
  الأعداد تظهر في `/archive` نفسها. تحقق: `tsc --noEmit` نظيف، ومجموعة الواجهة
  536/536 ناجحة (تشمل 5 اختبارات جديدة لـ`isIncompleteRecord` و`WORK_LISTS`).
  لم تُعَد البوابة الحية لهذا البند (تعديل واجهة فوق منطق مُختبَر وحدويًا)، على
  نفس نهج V1-824/V1-826.
- [x] **V1-826 استئناف مسودة التحرير** — تم؛ `lib/edit-draft-position.ts` يحفظ
  (معرّف المادة، اسم الحقل) في `localStorage` عند تركيز أي حقل في نموذج
  «تحرير التوصيف» (`RecordDescribeForm`)، ويُستعاد عند العودة لنفس المادة
  بتركيز الحقل وتمرير الشاشة إليه فقط — من دون تغيير أي قيمة محتوى؛ يُمسح
  الموضع المحفوظ عند الحفظ الصريح الناجح. (لا مفهوم "تبويب" منفصل في هذه
  الصفحة حاليًا، فاقتصر النطاق على المادة والحقل). تحقق: `tsc --noEmit` نظيف،
  ومجموعة الواجهة 531/531 ناجحة (تشمل 4 اختبارات جديدة للمخزن نفسه).
- [x] **V1-827 قوالب البيانات الوصفية** — تم بتخزين قاعدي (`metadata_templates`
  جدول Laravel). `MetadataTemplatesController` (index بفلتر typeId/store/update/
  destroy)، وعقد OpenAPI وعميل Next.js مطابقان. «معاينة ما سيطبق» دالة نقية
  `lib/metadata-template-apply.ts` (`previewTemplateApplication`) تملأ الحقول
  الفارغة فقط من القالب ولا تستبدل ما أدخله المستخدم. تحقق: 3/3 PHPUnit ناجحة،
  2/2 vitest ناجحة، `verify:api-contracts` ناجح.
- [x] **V1-828 إجراءات جماعية آمنة** — الوسوم والحالة والحذف كانت مغطاة أصلًا عبر
  ميزة bulk-macros القائمة (معاينة، نتائج لكل عنصر، تدقيق). أُضيف نوع خطوة جديد
  `set-rights-holder` (`BulkMacroService::applyRightsHolderStep`) يكتب في جدول
  `rights_records` عبر مسار تنفيذ خاص به، لإغلاق فجوة «مالك/حق». «نقل» لم يُبنَ:
  لا مفهوم نقل/إعادة أصل قائم في هذا الكود ليُبنى عليه. تحقق: 2/2 PHPUnit جديدة
  ناجحة، مجموعة BulkMacrosApiTest الكاملة (16 اختبارًا) خضراء بلا تراجع.
- [x] **V1-829 السجل الزمني الموحّد للمادة** — تم؛ `lib/record-timeline.ts`
  (`buildRecordTimeline`) يدمج السجل والتعليقات والحقوق المحمّلة أصلًا في صفحة
  المادة إلى قائمة واحدة مرتّبة زمنيًا (الأحدث أولًا) — بلا نظام نشاط جديد.
  تحقق: 4/4 اختبارات جديدة ناجحة.
- [x] **V1-830 تنبيهات سلامة داخل سياق المادة** — تم؛ `lib/record-safety-alerts.ts`
  (`buildSafetyAlerts`) يكشف الملفات المرفقة المتطابقة بالـchecksum، والحقوق
  المنتهية، والرفعات غير المكتملة — من بيانات صفحة المادة المحمّلة أصلًا، بلا
  مزود ذكاء اصطناعي. رابط المشاركة القديم لم يُغطَّ: لا بيانات مشاركة محمّلة في
  هذه الصفحة حاليًا. تحقق: 5/5 اختبارات جديدة ناجحة.
- [x] **V1-831 نسخ مادة كمسودة** — تم؛ `lib/record-duplicate.ts`
  (`buildDuplicateDraftPayload`) يبني حمولة لمسار `POST /records` القائم — عنوان
  مسبوق لإشارة النسخ، وصف/نوع/وسوم منسوخة، مصدر النسخة مسجَّل في
  `metadata.duplicatedFrom` للتتبع. لا ملفات أو روابط مشاركة أو حقوق حساسة تُنسخ
  لأن ذلك المسار لا يقبلها أصلًا. تحقق: 4/4 اختبارات جديدة ناجحة.
- [x] **V1-832 اختصارات العمل للمستخدم الخبير** — تم؛ كان الموجود اختصارين فقط
  (لوحة الأوامر ولوحة المساعدة)، وأُضيفت الخمسة المطلوبة: البحث `/`، مادة جديدة
  `n`، الحفظ `Ctrl/Cmd + Enter`، التعليقات `c`، الوسوم `t`. القيد الحاكم
  «لا تعترض اختصارات المتصفح» استبعد صراحةً `Ctrl+S` و`Ctrl+N`، فالحفظ صار على
  `Ctrl+Enter` الذي لا يطالب به متصفح، ويحرس ذلك اختبارٌ يرفض أي ارتباط يجمع
  مُعدِّلًا أساسيًا مع `s/n/t/w/p`. الجذر المُصلَح: حارس الكتابة `isTypingTarget`
  كان خاصًا داخل `ShortcutsOverlay` وحده، فأي اختصار جديد كان سيصل بلا حراسة
  ويبتلع ضغطة أثناء الكتابة؛ نُقل إلى `lib/keyboard-shortcuts.ts` ويستدعيه كل
  المستهلكين. وكشف الاختبار أن الحارس كان يُعيد `undefined` (لا `boolean`) على
  عناصر `contenteditable` لأن الخاصية غير منفَّذة في jsdom، فأُضيف رجوع إلى
  السمة نفسها. الاختصارات أحادية الحرف محروسة، و`Ctrl/Cmd + Enter` يعمل عمدًا
  داخل الحقول لأن الحفظ من داخل النموذج هو الغرض. تحقق: `tsc --noEmit` نظيف،
  ومجموعة الواجهة 554/554 ناجحة (6 اختبارات جديدة).
- [x] **V1-834 عرض الفروقات والاستعادة** — تم؛ لم توجد بنية لالتقاط القيم السابقة
  على مستوى الحقل في أي مكان بالنظام، فبُنيت من الصفر: جدول جديد
  `record_metadata_snapshots` يُملأ من نقطة الكتابة المشتركة الوحيدة
  (`RecordsController::bulk`) — نسخة كاملة من الصف *قبل* الكتابة فقط عند وجود
  سجل سابق (لا عند الإنشاء)، ضمن `try/catch` لا يفشل أبدًا الحفظ الفعلي إن فشل
  الالتقاط. `RecordSnapshotsController` يقرأ (قائمة لقطات، فرق حقل-حقل بين
  لقطة والحالي) ويستعيد (`title`/`description`/`type`/`subtype`/`tags` فقط —
  لا ملفات ولا حقوق ولا روابط مشاركة)، وكل استعادة تلتقط لقطة لحالتها هي أيضًا
  فتبقى قابلة للتراجع. المخاطرة الحقيقية كانت لمس مسار الكتابة المشترك الذي
  تعتمد عليه الأتمتة والاستيراد والمزامنة؛ عولجت بإبقاء الإضافة إضافية بحتة
  (جدول جديد، محاولة معزولة بلا أثر على تدفق الكتابة نفسه) والتحقق الحي الكامل
  لكل الاختبارات التي تمر عبر `bulk()` (Records/RoleMatrix/Delegated/
  EventDrivenAutomation/BulkMacros/Trash — 53 اختبارًا) قبل الإغلاق. تحقق: 5/5
  اختبارات جديدة ناجحة، ومجموعة PHPUnit الكاملة خضراء بلا أي فشل.
- [x] **V1-835 سجل نتائج العملية الجماعية** — تم؛ الجزء الأول (نتيجة قابلة
  للبحث لكل تنفيذ) كان مغطى أصلًا (`GET /bulk-macros/{id}/runs`). الجزء
  الناقص: `BulkMacroService::retryFailed` (مسار جديد `POST
  /bulk-macros/{id}/runs/{runId}/retry-failed`) يعيد فقط العناصر التي فشلت أو
  اكتملت جزئيًا، مع إعادة التحقق من الصلاحية (نفس حارس `requireEditor`) ومن
  الحالة الحالية (يعيد `processTarget` قراءة كل صف من جديد، فسجل حُذف فعليًا
  منذ التشغيل السابق يظهر الآن "missing" لا "مفترض موجودًا"). التشغيل الجديد
  مرتبط بالأصلي عبر عمود `retried_from_run_id`. تحقق: 2/2 PHPUnit جديدة ناجحة،
  مجموعة BulkMacrosApiTest الكاملة خضراء.
- [x] **V1-836 فحص قبل المشاركة** — تم لصفحة `/files`؛ زر «إنشاء رابط مشاركة» كان
  يستدعي `api.createShare` فورًا بلا أي `expiresAt` (كانت البارامتر مدعومة في
  العميل والخادم منذ البداية لكن غير مستخدمة إطلاقًا في الواجهة) وبلا أي تحقق.
  الآن يفتح لوحة فحص (`lib/share-checklist.ts` لمنطق التاريخ والتحقق، بلا آثار
  جانبية): حقل تاريخ انتهاء افتراضيًا +7 أيام قابل للتعديل مع رفض الماضي/الفارغ،
  وصندوقا تأكيد (سريان الحقوق، خلو الملفات من بيانات حساسة) لا يُفعَّل زر
  «تأكيد ومشاركة» إلا بعد تحقيقهما كليهما — القرار النهائي يبقى للمستخدم صراحةً
  عبر هذين التأكيدين بدل فحص آلي قد يخطئ. لم يُبنَ فحص حقوق تلقائي عبر استدعاء
  API لكل ملف لأن `ArchiveFile` لا يحمل ربطًا مباشرًا بسجل الحقوق (لا يوجد
  `itemId` موثوق لكل ملف تخزين خام)، فتحويله فحصًا تلقائيًا كان سيحتاج بنية
  ربط جديدة تتجاوز نطاق هذا البند. تحقق: `tsc --noEmit` نظيف، ومجموعة الواجهة
  540/540 ناجحة (تشمل 4 اختبارات جديدة لمنطق `share-checklist`).
- [x] **V1-837 وضع الفرز السريع** — القبول والوسم والتأجيل كانت مغطاة أصلًا (حالة
  سير العمل، الوسوم، وقائمة «لاحقًا» V1-842). الإشارة الوحيدة الناقصة فعليًا:
  علامة «بحاجة لمعلومة». تم بتخزين قاعدي (`record_triage_flags` جدول Laravel،
  علامة واحدة ظاهرة لكل الفريق لكل مادة) عبر `RecordTriageFlagController`
  (show/upsert/destroy). واجهة الفرز المتتابع بلوحة مفاتيح لم تُبنَ بعد — هذا
  البند يغطي الطبقة الخلفية فقط، والواجهة متابعة لاحقة فوقه. تحقق: 5/5 PHPUnit
  ناجحة، `verify:api-contracts` ناجح.
- [x] **V1-838 المفضلات والسياق الحديث** — تم؛ الوصول السريع كان قائمًا أصلًا
  (`lib/favorites.ts` و`lib/recent-items.ts` و`lib/recent-searches.ts` معروضة في
  `/daily` و`/favorites` واقتراحات البحث)، والناقص فعليًا هو شقّ «المسح والتحكم
  بالظهور»: `clearRecent()` كانت دالة ميتة لا يستدعيها أي واجهة، ولا وجود لمسح
  عمليات البحث الأخيرة أصلًا. أُضيف `lib/personal-context.ts` (مفتاح انسحاب واحد
  في `localStorage`) يقرأه **مسارا الكتابة معًا** — `recordView` و
  `recordRecentSearch` — فالإيقاف يمنع التسجيل من المصدر لا من العرض، ثم
  `clearRecentSearches()`، وضوابط في لوحة «آخر ما شاهدت» في `/daily`: مفتاح
  التسجيل، ومسح السجل، ومسح عمليات البحث، مع نص صريح بأن السياق محفوظ في هذا
  المتصفح وحده ولا يُشارك. لم يُضف مسح جماعي للمفضلات: المفضلة تثبيت صريح
  ويُلغى فرديًا، والمسح الشامل إتلاف غير مطلوب. تحقق: `tsc --noEmit` نظيف،
  ومجموعة الواجهة 543/543 ناجحة (تشمل 3 اختبارات جديدة تثبت أن الانسحاب يوقف
  المسجّلين معًا وأن مسح أحد المخزنين لا يمس الآخر).
- [x] **V1-839 دمج المواد المكررة** — تم؛ `RecordMergeController` (معاينة +
  دمج) ينقل الوسوم (اتحاد)، ويعيد ربط `record_relations` (يُسقط الروابط التي
  كانت ستصبح ذاتية أو تتعارض مع رابط فريد قائم على المادة الرئيسية)، ويعيد
  ربط ملاحظات وتعليقات المادة المكررة إلى الرئيسية. المادة المكررة تُنقل إلى
  سلة المهملات عبر آلية `TrashController::trashRow` القائمة بدل بناء آلية
  استعادة جديدة — «خيار استعادة مضبوط» هو مسار `/trash/restore` نفسه. لا تُمس
  ملفات المادة المكررة إطلاقًا. تحقق: 4/4 اختبارات PHPUnit جديدة ناجحة.
- [x] **V1-841 تدقيق الروابط الداخلية** — تم؛ `LinkAuditController` (قراءة فقط)
  يفحص جدول `record_relations` القائم ويكشف كل علاقة مصدرها أو هدفها لم يعد
  موجودًا في `storage_rows` — بلا تخزين جديد. تحقق: 2/2 اختبارات جديدة ناجحة.
- [x] **V1-842 قائمة «لاحقًا»** — تم؛ `lib/later-list.ts` يخزّن التأجيل في
  `localStorage` (سبب إلزامي وموعد مراجعة اختياري) بنفس نمط `favorites.ts`،
  لا خدمة مهام جديدة ولا استدعاء API. زر «لاحقًا» في صفحة المادة يفتح نموذجًا
  مصغّرًا (سبب + تاريخ)، ويتحول الزر لـ«إلغاء التأجيل» بعد الحفظ. مادة بلا موعد
  مراجعة تُعتبر مستحقة فورًا (`isDue`)؛ بموعد، تُستحق عند بلوغه أو تجاوزه.
  لوحة «لاحقًا» الجديدة في `/daily` تعرض المواد المستحقة فقط (لا كل المؤجَّل)
  مع رابط للمادة وزر إزالة — هذا هو «الإظهار التلقائي ضمن قوائم عمل المستخدم».
  تحقق: `tsc --noEmit` نظيف، ومجموعة الواجهة 560/560 ناجحة (تشمل 6 اختبارات
  جديدة لمنطق `later-list`).
- [x] **V1-843 اقتراح الحقول الناقصة عند الحفظ** — تم؛ `missingDescribeFields`
  في `lib/record-status.ts` يحكم على **المسودة غير المحفوظة** بنفس الحقول
  الأربعة التي يحسبها `descriptorCompletion` في الـAPI، فلا تتعارض الشارة
  (V1-851) مع النموذج. بعد الحفظ الناجح تُلحق رسالة موجزة تسمّي ما بقي ناقصًا
  («تم حفظ التوصيف. ما زال ينقصه: …») بلا حظر. وأثناء ذلك انكشف عيب قائم:
  `handleSubmit` كان يبدأ بـ`if (busy || !title.trim()) return;` — أي أن الضغط
  على «حفظ» بعنوان فارغ لا يفعل شيئًا ولا يقول شيئًا، فيبدو الزر معطّلًا.
  العنوان هو القاعدة الإلزامية الوحيدة المعتمدة فعلًا في هذا النموذج، فبقي
  المنع وأُضيف سببه صراحةً بدل الصمت. تحقق: `tsc --noEmit` نظيف، ومجموعة
  الواجهة 549/549 ناجحة (يشمل اختبارًا جديدًا يثبت أن الدالة تسمّي الحقول
  الفارغة وحدها).
- [x] **V1-845 سلة عمل مؤقتة** — تم؛ `lib/work-basket.ts` يخزّن السلة في
  `localStorage` بنفس نمط `later-list.ts`/`favorites.ts` (لا خدمة مهام جديدة
  ولا استدعاء API). زر «أضف إلى السلة» في صفحة المادة يبدّل عضوية السجل، ولوحة
  «سلة العمل» الجديدة في `/daily` تعرض المجموعة مع إزالة فردية أو «تفريغ
  السلة» الكاملة — نقطة الإضافة الوحيدة هي صفحة المادة، بنفس نطاق المفضلة
  والتأجيل الحاليين (لا تُضاف أزرار سلة إلى صفوف نتائج البحث أو القوائم، إذ لا
  توجد هناك أصلًا لأي من الميزتين المشابهتين). التسليم إلى V1-828 يبقى تكاملًا
  مستقبليًا لأن تلك الميزة غير موجودة بعد. تحقق: `tsc --noEmit` نظيف، ومجموعة
  الواجهة 565/565 ناجحة (تشمل 5 اختبارات جديدة لمنطق `work-basket`).
- [x] **V1-847 استيراد البيانات الوصفية من CSV** — ربط أعمدة ملف CSV بحقول النظام
  القائمة، وعرض معاينة وأخطاء لكل صف قبل التطبيق، وتسجيل دفعة الاستيراد ونتيجتها
  في التدقيق؛ لا ينشئ حقولًا مخصصة ضمن هذا النطاق. التحقق 2026-07-31:
  15 اختبارًا مركّزًا ناجحًا.
- [x] **V1-848 مطالبة المادة للتحرير** — تم؛ `record_edit_claims` جدول Laravel،
  `RecordEditClaimController` (show/claim/release)، صلاحية 5 دقائق، إعلامي فقط
  — لا يمنع الحفظ أبدًا. أي محرر موثّق يستطيع المطالبة فوق مطالبة منتهية أو
  لمستخدم آخر (التجاوز الموثق المطلوب صراحةً). المرجع النهائي لتضارب التعديلات
  يبقى فحص `syncVersion` القائم عند الحفظ، لم يتغيّر. تحقق: 3/3 اختبارات جديدة
  ناجحة.
- [x] **V1-849 ملاحظات خاصة بالمستخدم** — تم إغلاق الفجوة الحقيقية الموجودة: ميزة
  «ملاحظاتي» في `RecordNotesPanel` كانت تفترض الخصوصية بصريًا (التسمية والتصميم)
  بينما `RecordNotesController::index()` في Laravel كان يعيد ملاحظات **كل**
  المستخدمين على السجل دون أي فلترة بالمؤلف — أي مستخدم موثّق يرى ملاحظات
  الجميع الأخرى. الإصلاح: تصفية القائمة بـ`author_id` للمستخدم الحالي، مع
  استثناء للمدير (يرى الكل لأغراض الإشراف، مطابقًا لصلاحية التعديل/الحذف
  الموجودة أصلاً). الحذف والتعديل كانا مقيَّدين بشكل صحيح مسبقًا (لا حاجة
  لتغيير). لم تُبنَ إعدادات "احتفاظ" منفصلة — لا توجد بنية إعدادات مستخدم لهذا
  النوع من التفضيلات بعد، وإضافتها الآن تكهّن غير مطلوب. **ملاحظة متابعة غير
  مغلقة**: الإشارة بـ"@" داخل ملاحظة خاصة (`MentionTextarea` في نموذج
  الملاحظات) ما زالت تُرسل إشعارًا للمُشار إليه رغم أنه لن يستطيع رؤية الملاحظة
  بعد هذا الإصلاح (خصوصية حقيقية الآن) — تناقض تصميمي قائم من قبل هذا الإصلاح
  وليس ناتجًا عنه، يستحق مهمة مستقلة إذا أُريد حله. تحقق: 7/7 اختبارات
  `RecordNotesApiTest` ناجحة (يشمل اختبارًا جديدًا للخصوصية)، ومجموعة PHPUnit
  الكاملة خضراء بلا أي فشل.
- [x] **V1-850 إعادة ربط المراجع** — تم؛ `VocabularyRelinkController` يعرض كل
  المواد الموسومة بمصطلح قبل حذفه أو تغييره (`GET /vocabulary/{id}/relink-preview`)
  ثم يستبدل الوسم أو يزيله عبر كل المواد المتأثرة في خطوة مراجَعة واحدة
  (`POST .../relink`) بدل ترك مرجع معطّل صامتًا؛ يحذف المصطلح بعد إعادة الربط.
  تحقق: 3/3 اختبارات جديدة ناجحة، مجموعة VocabularyApiTest القائمة خضراء بلا
  تراجع.
- [x] **V1-851 حالة المادة المختصرة** — تم؛ `lib/record-status.ts`
  (`deriveRecordStatus`) يشتق حالة واحدة من مصدرين موجودين فقط: حالة سير العمل
  و`descriptorCompletion` القادم من الـAPI — بلا دورة حالات موازية وبلا حقل
  مخزَّن جديد، التزامًا بإحالة V1-824. الترتيب: مؤرشفة ثم قيد المراجعة ثم ناقصة
  ثم مسودة ثم جاهزة؛ **النقص يتقدّم على «منشور»** عمدًا لأنه الحالة القابلة
  للتنفيذ، وإخفاؤه خلف حالة سير العمل هو تحديدًا كيف تبقى المواد ناقصة الوصف.
  كل حالة تحمل سببًا صريحًا (الحالة الناقصة تسمّي الحقول الغائبة نصًّا). حلّت
  الشارة محل شارة `descriptorCompletion` المحلية في بطاقة الأرشيف، وأُضيفت إلى
  لوحة جاهزية المادة في صفحة المادة، فصار المصدر واحدًا في الموضعين. تحقق:
  `tsc --noEmit` نظيف، ومجموعة الواجهة 548/548 ناجحة (تشمل 5 اختبارات جديدة
  تثبت الترتيب وتسمية الحقول الناقصة ووجود سبب غير فارغ لكل حالة).
- [x] **V1-853 قائمة الملفات غير المستخدمة** — تم؛ `UnusedFilesController` (قراءة
  فقط) يفحص جذر ملفات الأرشيف المُهيّأ ويقارنه بجدول `record_attachments`
  القائم، ويعرض كل ملف بلا مرجع إرفاق مع سبب الاكتشاف — بلا حذف تلقائي. لا
  يقارن مع مسارات المصدر المضمّنة القديمة في JSON بعض السجلات (نطاق مستبعد
  صراحةً، لا تجاهل صامت). تحقق: 2/2 اختبارات جديدة ناجحة.
- [x] **V1-854 ترحيل المجلد المحلي المراقَب للمسار القانوني** — تم؛ الميزة
  القانونية في Laravel + Next.js مع ربط Docker محدود المسار، فحص استقرار الملف،
  دفعة مرئية ومعاينة ثم اعتماد صريح، checksum وسجل تدقيق مركزي، وعقد OpenAPI
  وعميل Next.js مولّدين. لا مزود تخزين أو خدمة خارجية جديدة.
- [x] **V1-855 قواعد فرز المجلد المراقَب** — تم؛ قواعد CRUD محكومة تربط بادئة
  المسار أو نمط الاسم بقالب ووسوم ووجهة تجهيز. تظهر المطابقة في معاينة الدفعة
  ولا تُطبق إلا بعد الاعتماد الصريح، مع دعم الملفات المتداخلة.
- [x] **V1-856 منطقة حجر للاستيراد** — تم؛ تعزل معاينة الدفعة تضارب checksum
  قبل الاعتماد، وينقل فشل التحقق الملف إلى الحجر. تظهر الحالة والسبب في جدول
  الدفعة للمراجعة ولا يُنشأ سجل أرشيف للملف المعزول.
- [x] **V1-857 معاينة دفعة الإدخال** — تم؛ `lib/import-batch-preview.ts`
  (`buildImportBatchPreview`) دالة نقية تعرض الاسم/الحجم/المدة/checksum لكل
  ملف وتكشف التكرار المتوقع (مقابل الأرشيف القائم وداخل الدفعة نفسها) — قابلة
  لإعادة الاستخدام مع أي مصدر استيراد (مجلد مراقَب، CSV، FTP/SMB/Dropbox،
  رفع يدوي) دون افتراض ناقل بعينه. تحقق: 4/4 اختبارات جديدة ناجحة.
- [x] **V1-860 تاريخ صحة الملف** — تم؛ جدول `file_health_checks` +
  `FileHealthController` (سجل الفحوصات + تشغيل فحص جديد) يعيد حساب sha256
  ويقارنه بـ`checksum_sha256` المخزَّن في `record_attachments` عند الطلب —
  match/mismatch/missing/error. تحقق: 4/4 اختبارات جديدة ناجحة.
- [x] **V1-862 طابور تجهيز شخصي** — تم؛ `lib/personal-queue.ts` يخزّن الطابور في
  `localStorage` بنفس نمط `work-basket.ts` مع إضافة `moveInQueue` للترتيب
  اليدوي (أعلى/أسفل، بلا أثر عند طرفي القائمة). زر «أضف إلى الطابور» في صفحة
  المادة يبدّل العضوية، ولوحة «طابور التجهيز الشخصي» في `/daily` تعرض الترتيب
  مع أزرار تحريك لكل عنصر وإزالة فردية أو «تفريغ الطابور» الكاملة — منفصل عن
  حالة المادة الرسمية وعن سلة العمل المؤقتة V1-845، إذ يضيف بُعد الترتيب الذي
  لا تملكه السلة. تحقق: `tsc --noEmit` نظيف، ومجموعة الواجهة 572/572 ناجحة
  (تشمل 7 اختبارات جديدة تثبت الإضافة والاستبدال والإزالة والتبديل والتفريغ
  والتحريك في الاتجاهين وعدم التأثر عند الطرفين).
- [x] **V1-865 استبدال ملف المصدر مع حفظ هوية المادة** — تم؛ الاستبدال يحتفظ
  بالمعرّف والعلاقات، يسجل المصدر السابق ويعرض نسخاً قابلة للاستعادة، ويعيد
  تشغيل مشتق الوسائط بأمان عبر المسار القائم.
- [x] **V1-866 تجميد المادة للمراجعة** — تم؛ جدول `record_freezes` +
  `RecordFreezeController`، والإنفاذ في `RecordsController::bulk` نفسه (مسار
  الكتابة المشترك) — سجل مجمَّد يُسقط الكتابة إليه صامتًا ويُدرجه في مصفوفة
  `blocked` الجديدة في استجابة `bulk`، بدل الاكتفاء بمنع الواجهة فقط. التجاوز
  الموثق: المدير يتجاوز التجميد. تحقق: 3/3 اختبارات جديدة ناجحة، وكل الاختبارات
  التي تمر عبر `bulk()` (84 اختبارًا) خضراء بلا تراجع.
- [x] **V1-868 مصدر القيمة الوصفية** — تم؛ جدول `record_field_sources` يُملأ
  اختياريًا من حقل `fieldSources` جديد في حمولة `RecordsController::bulk`
  (manual/template/csv/bulk) — يُستبعد من JSON السجل المخزَّن فلا يتسرب إليه،
  ومستدعٍ لا يرسله لا يترك أي أثر في الجدول. تحقق: 4/4 اختبارات جديدة ناجحة،
  وكل الاختبارات التي تمر عبر `bulk()` (129 اختبارًا) خضراء بلا تراجع.
- [x] **V1-869 معالجة البحث بلا نتائج** — تم بشقّيه. `lib/search-suggestions.ts`
  (`suggestNearMatches`) مطابقة تقريبية نقية (Levenshtein) على فهرس محمّل أصلًا
  (وسوم/تصنيفات) بلا خدمة إملاء خارجية. `lib/failed-search-log.ts` سجل محلي
  محدود (50 عبارة، `localStorage`) — النص نفسه يطلب «تسجيل محلي» صراحةً، فلا
  حاجة لجدول قاعدي. تحقق: 10/10 اختبارات جديدة ناجحة.
- [x] **V1-870 معاينة أثر التعديل أو الحذف** — تم؛ معاينة قراءة فقط تعرض
  العلاقات والمشاركات والمقاطع والتقارير قبل الحذف، وتحجب القرار عند وجود
  علاقات غير محلولة وتسجله في التدقيق.
- [x] **V1-871 قاموس مصطلحات معتمد** — تم؛ قيمة رئيسية للبدائل في القاموس
  القائم مع بقاء البدائل قابلة للبحث، وتستفيد من معاينة وإعادة ربط V1-850
  المدققتين بدلاً من حذف مباشر.
- [x] **V1-872 طلب معلومة مفقودة** — تم؛ جدول `record_field_requests` +
  `RecordFieldRequestController` — طلب مرتبط بحقل محدد (مسؤول + تاريخ متابعة +
  حل)، ومسار عابر للمواد `GET /field-requests` (بفلتر مسؤول، يستبعد المحلول)
  لعرضها في قوائم العمل — بلا منصة مهام جديدة ولا مراسلة خارجية. تحقق: 4/4
  اختبارات جديدة ناجحة.
- [x] **V1-873 مجموعات وعمليات بحث محفوظة قابلة للتحرير** — تم جزئيًا؛
  `CollectionsController::update` يعيد تسمية المجموعة ويعدّل معاييرها (بدل
  الاكتفاء بالإنشاء)، وجدول جديد `collection_records` يضيف عضوية صريحة من
  مواد مباشرة إلى جانب المعيار الديناميكي القائم (بحث/نوع/وسم) — يغطي «من
  مواد» و«من نتيجة بحث» معًا. صلاحيات مالك/محرر/مشاهد للمشاركة **لم تُبنَ** —
  هذا تحديدًا نطاق V1-878 المستقل. تحقق: 4/4 اختبارات جديدة ناجحة، ومجموعة
  RoleMatrixApiTest الكاملة (44 اختبارًا) خضراء بلا تراجع.
- [x] **V1-874 إدارة قوالب البيانات حسب القسم** — تم؛ مكتبة مركزية تفرض قسمًا
  مالكًا عند الإنشاء، وتحفظ لقطات غير قابلة للتغيير لكل إصدار، وأدوار استخدام
  وتعطيلًا صريحًا. تعرض الواجهة القسم والقيم والمعاينة وسجل الإصدارات؛ لا
  تعدّل المواد القائمة بأثر رجعي.
- [x] **V1-875 نشر قالب بعد اعتماد القسم** — تم؛ المسودة لا تظهر للمستخدمين
  حتى يعتمدها المسؤول (`admin` في نموذج الأدوار الحالي)، ويحفظ النشر الإصدار
  والناشر والوقت، مع استعادة أي إصدار سابق نُشر بعد مراجعة صريحة.
- [x] **V1-876 قواعد جودة لكل قسم** — تم؛ قواعد حقول مطلوبة بحسب القسم والنـوع
  مع معاينة صريحة لأسباب عدم الجاهزية، ولا تطبّق قاعدة قسم على آخر. تظهر
  وتُدار من مكتبة القوالب المركزية من دون تعديل رجعي للسجلات.
- [x] **V1-877 توجيه الوارد حسب القسم** — مكتمل: التوجيه اليدوي يفرض معاينة
  صريحة قبل الكتابة ويسجل سلسلة الأقسام، ويمنع تكرار القسم أو الحلقة أو أكثر من
  ثماني نقلات. قواعد الأتمتة التي تنشئ متابعة تكتب في صندوق الوارد المركزي نفسه
  وتستطيع تحديد قسم مستهدف. التحقق: 22 اختبار Laravel/149 assertion، وعقد API
  وفحص أنواع Next.js ناجحة.
- [x] **V1-878 صلاحيات المجموعات المحفوظة** — مكتمل: قائمة وصول صريحة بأدوار
  مالك ومحرر ومشاهد لكل بحث محفوظ، مع قسم اختياري وسجل تدقيق لاستبدال العضوية.
  لا تظهر المجموعة إلا للمالك أو لعضو مُضاف صراحةً، ويمكن للمحرر إدارة العضوية
  دون حذف المجموعة. التحقق: 15 اختبار Laravel/75 assertion، وعقد API وفحص
  أنواع Next.js ناجحة.
- [x] **V1-879 مالك الحقل حسب القسم** — مكتمل: سجل مركزي لمسؤولي الحقول لكل
  قسم، مع دعم `*` كمسؤول افتراضي. يظهر المسؤول في طلب المعلومات ويُسند إليه
  تلقائيًا عند غياب إسناد صريح، من دون منع المحرر المخوّل من التصحيح. التحقق:
  13 اختبار Laravel/58 assertion، وعقد API وفحص أنواع Next.js ناجحة.
- [x] **V1-880 مؤشرات استخدام القوالب** — مكتمل: مؤشرات مجمعة داخلية للقوالب
  المنشورة وقواعد الجودة والمواد والحقول الناقصة لكل قسم، مع عرض في مكتبة
  القوالب ومن دون أي خدمة تحليلات خارجية أو محتوى مواد فردي.
- [x] **V1-881 تسليم المادة بين الأقسام** — مكتمل: سجل تسليم مدقق يحفظ القسمين
  والمرسل والمستلم وقائمة الحقول المفتوحة وحالة الحقوق وعدد التعليقات؛ لا يغير
  مالك السجل أو صلاحياته.
- [x] **V1-882 مفردات مقيدة بالقسم** — مكتمل: تفضيلات صريحة ومدققة للقيم
  المعتمدة لكل قسم ترفعها في الاختيار والبحث، مع بقاء القاموس العام والبدائل
  القديمة قابلين للبحث وفق V1-871 ومن دون إنشاء قاموس منفصل لكل قسم. التحقق:
  17 اختبار Laravel/72 assertion، وعقد API وفحص أنواع وبناء Next.js ناجحة.
- [ ] **V1-307B قياسات الواجهة** — LCP/CLS/INP على جهاز baseline. **محجوب قدرة:**
  لا يوجد جهاز `rc-baseline-linux-x64` (Ubuntu 24.04، 4 vCPU، 8 GiB) أو دليل
  تشغيل فعلي؛ الجهاز المحلي Windows 11/28 خيطاً/31.7 GiB ولا يجوز نسب نتائجه
  إلى baseline.
- [ ] **V1-307C قياسات API والرفع** — P95 للبحث وفتح السجل وبدء الرفع.
  **محجوب قدرة:** يلزم تشغيل Docker وNative على جهاز baseline نفسه مع بيانات
  V1-307A الكاملة؛ لا توجد artifacts قياس فعلية قابلة للمراجعة في المستودع.
- [ ] **V1-307D بوابة regression** — baseline قابل للمقارنة ومنع تجاوز budget.
  **محجوب قدرة:** بوابة العقد موجودة وتنجح محلياً، لكن لا يمكن اعتماد baseline
  أو تفعيل مقارنة نتائج حقيقية قبل إتاحة evidence لعشرين عينة لكل مقياس.

## مهام مستوردة من CloudMediaDB — Archive Suite

> أضيفت كخريطة عمل مرجعية؛ لا تغير حالة البنود الموجودة ولا تعني أن أي قدرة
> مكتملة قبل التحقق منها في المسار القانوني Laravel + Next.js.

### أولوية P0 — نواة الأرشيف والحوكمة

- [ ] **AS-01 سجل المادة الأرشيفية الموحد** — توحيد بيانات الملفات والبيانات الوصفية والتصنيفات والوسوم وحالة الاعتماد.
- [ ] **AS-02 التصنيف الهرمي والحقول المخصصة** — دعم الفئات المتداخلة، وترتيب الحقول، والإلزام، والظهور المشروط.
- [ ] **AS-03 قاموس المصطلحات المعتمد** — إدارة الأشخاص والأماكن والأحداث والمرادفات وربطها بالمواد.
- [ ] **AS-04 الصلاحيات والتدقيق** — أدوار المستخدمين، جلسات آمنة، صلاحيات الحقول الحساسة، وسجل عمليات قابل للمراجعة.
- [ ] **AS-05 إدارة دورة حياة المادة** — إدخال يدوي، مراجعة واعتماد، نسخ تاريخية، حذف قابل للاسترجاع، ومفضلة.

### أولوية P1 — الإدخال والبحث والإثراء

- [ ] **AS-06 رفع واستيراد الوسائط** — رفع ملفات كبيرة على أجزاء، استيراد الملفات والترجمة SRT، وربط التخزين السحابي بعد مراجعة الأمان.
- [ ] **AS-07 البحث والفلاتر المتقدمة** — بحث نصي وفلترة حسب النوع والتصنيف والوسوم والتاريخ وحالة اكتمال التوصيف.
- [ ] **AS-08 صفحة تفصيل المادة والتعاون** — معاينة المادة، تحرير الوصف، التعليقات، وإسناد المهام المرتبطة بها.
- [ ] **AS-09 خدمات الذكاء الاصطناعي المساعدة** — التفريغ، التلخيص، اقتراح الوسوم، استخراج الكيانات، والتدقيق اللغوي مع إبقاء المراجعة البشرية إلزامية.

### أولوية P2 — سير العمل والإنتاج

- [ ] **AS-10 المجموعات والمشاريع** — تجميع المواد في حقائب عمل ومشاريع مع ملاحظات وترتيب واضح.
- [ ] **AS-11 مهام المشروع ولوحة كانبان** — حالات، مسؤول، تاريخ تحديث، وربط اختياري بمادة أرشيفية.
- [ ] **AS-12 إعدادات المقاطع وخرائط NLE** — نقاط بداية/نهاية، تقسيم المقاطع، Rough Cut، وتصدير JSON منظم لبرامج المونتاج.
- [ ] **AS-13 مشاركة المشاريع** — روابط مشاركة مضبوطة بمدة وصلاحيات مناسبة.

### أولوية P3 — الاستكشاف والتقارير والتشغيل

- [ ] **AS-14 عرض شبكة العلاقات** — إظهار الصلات بين المواد والمصطلحات والأشخاص والأماكن.
- [ ] **AS-15 الخريطة والخط الزمني** — استكشاف المواد بحسب الموقع والحدث والفترة الزمنية.
- [ ] **AS-16 مركز التقارير والتصدير** — تقارير تشغيلية ومؤشرات جودة التوصيف وتصدير Excel وCSV وPDF.
- [ ] **AS-17 النسخ الاحتياطي واستعادة البيانات** — نسخ قاعدة البيانات، استيرادها، والتحقق من سلامة العملية.
- [ ] **AS-18 مراجعة أمن التكاملات السحابية** — حماية مفاتيح الخدمات، فرض المصادقة على كل نقطة نهاية، وإلغاء نقاط التشخيص المكشوفة قبل النقل.

## محجوبة خارجيًا — لا تُغلق محليًا

- [ ] **V1-X01** تحقق حي للتخزين الخارجي (Dropbox/S3): اعتماد حي، ملف كبير،
  انقطاع وإعادة محاولة، وعدم تسرب الاعتماد.
- [ ] **V1-X02** تحقق ODBC حي على Windows: DSN/driver وبيانات اختبار.
- [ ] **V1-X03** تفريغ عربي على GPU بعينة معتمدة ومقاييس.
- [ ] **V1-X04** AI/vision/embeddings بمزود وفهرس حيَّين.
- [ ] **V1-210D** قبول Windows نظيف (10/11، online/offline، update/rollback).
- [ ] **V1-806–V1-814** برنامج القبول متعدد المنصات: عقود providers، مصفوفة
  دورة الحياة، رحلات المدير/الموظف/المونتاج، التزامن، الأحمال، الجدولة، ولوحة
  أدلة القبول.
- [ ] **V1-501–V1-505** Alpha/pilot/rehearsal: game-day وبيئات RC نظيفة وtriage.
- [ ] **V1-601–V1-605** Go/No-Go والإصدار والنشر والتنزيل والدعم.

## قواعد الإدارة

1. تُنقل المهمة إلى `ChangeLog.md` بعد تحققها فقط، مع commit ودليل الاختبار.
2. يُذكر الاعتماد الخارجي صراحةً بدل وضع علامة منجزة شكلية.
3. أي ملاحظة تدقيق غير تافهة تُنشأ لها مهمة مستقلة قبل التنفيذ.
4. **قاعدة إعادة الفتح:** يُعاد فتح أي بند منقول إذا كشف تحقق لاحق خللًا في
   نطاقه أو فقد دليله أو تغيّر عقده العام.
