# مهام Masar الحالية

> هذا هو سجل **المهام المفتوحة فقط** للمسار القانوني Laravel + Next.js. تفاصيل كل بند مكتمل مؤرشفة في [`ChangeLog.md`](ChangeLog.md) ولا تعود هنا إلا بمتابعة جديدة ذات معرّف مستقل.

> **للوكلاء المنفذين:** تبدأ كل ميزة بـTDD، وتبقى في ملفاتها المملوكة. تُنفذ المميزات المتقاربة في دفعة واحدة، ثم تُراجع وتُفحص الدفعة قبل الدمج؛ ولكل ميزة مقبولة commit مستقل.

> **قاعدة إعادة الفتح:** لا يعود أي بند مكتمل إلى هذا السجل إلا بمتابعة جديدة ذات معرّف مستقل، أو بعد توفر شرط خارجي موثق يحوله إلى عمل محلي قابل للتحقق.

## الحالة

- الحالة العامة **NO-GO**: أُغلقت مانعات P0 الأصلية، لكن لا يزال الإصدار محجوبًا بالمهام المفتوحة، القبول الحي، وبوابات RC/GA.
- يوجد 40 بندًا مفتوحًا، **كلها مانعة للإصدار** — لا توجد بنود اختيارية أو معطلة افتراضيًا (قرار 2026-07-19: صلابة المعمارية V1-786/790 وتحققات القدرات الموسّعة V1-X01..X04 أصبحت إلزامية، راجع القسمين أدناه)؛ بوابة V1-406 تتولى منع الإصدار عند بقاء أي مانع مفتوح.
- التصميم الحاكم: [`docs/superpowers/specs/2026-07-14-daily-use-dual-runtime-design.md`](docs/superpowers/specs/2026-07-14-daily-use-dual-runtime-design.md).
- المسار القانوني للعمل الجديد هو `archive-next/` و`archive-laravel/`؛ أعمال التثبيت والتشغيل في `scripts/` و`infra/`.

## الأولويات التشغيلية وتبعيات الدفعات

> **الترتيب العام:** تطوير وتحسين المنتج أولًا (دفعات 2–4)، ثم الاختبار والفحص والقبول بكل أشكاله (دفعة 5) بعد إغلاق كل دفعات التطوير، ثم RC/GA أخيرًا. لا تُفتح دفعة اختبار جديدة قبل أن تُغلق دفعات التطوير المرتبطة بها.

| الترتيب | الدفعة | البنود المفتوحة | شرط البدء | بوابة الدمج |
| --- | --- | --- | --- | --- |
| 0 | تنظيف الحالة | تعديل غير ملتزم أو عمل قاطعته جلسة سابقة | جرد المالك والنطاق | فحص diff واختبار موجّه |
| 1 | أساس المنتج والعقود | مكتملة ومؤرشفة في ChangeLog | — | — |
| 2 | تجارب السجل والرفع والتعاون | بقية P8A/P8B/P8C ثم P8E/P8G | اكتمال العقد أو API المستهلك | build ولقطات/اختبارات الواجهات المتأثرة |
| 3 | السلامة والموثوقية والتكامل | P8D وP8F (V1-758 وV1-762) | لا تبدأ UI undo قبل V1-732 | Laravel/Node موجّهة ثم backup/health/queue checks |
| 4 | صلابة المعمارية وتمكين القدرات الموسّعة (إلزامية) | V1-786 وV1-790، وإزالة أعلام تعطيل القدرات الموسّعة (تخزين S3/Dropbox، ODBC، تفريغ GPU، AI/embeddings) تمهيدًا للتحقق الإلزامي V1-X01..X04 في الدفعة 5 | اكتمال الدفعتين 2 و3 | Laravel/Node موجّهة لكل تغيير عقد؛ الميزات الأساسية لهذه القدرات مبنية بالفعل (ODBC وWhisper وEmbeddings وDropbox/S3) — العمل هنا تمكين/تعميم لا بناء من الصفر |
| 5 | الاختبار والفحص والقبول | V1-303D..E وV1-306B..C وV1-307A..D، قبول Native خارجي (V1-210D/V1-211D)، برنامج القبول التفاعلي (V1-801..814)، والتحقق الحي الإلزامي للقدرات الموسّعة (V1-X01..X04) | **اكتمال دفعات التطوير 2–4** | live Playwright وaxe/screenshots/perf baseline ومصفوفة القبول وأدلة clean-host |
| 6 | RC وGA | V1-501..605 | اكتمال 1–5 | أدلة حية وتوقيعات Product/Security/Ops/Support |

### قواعد منع التعارض

- ملفات العقد المشتركة (`docs/api/archive-contract.openapi.json` و`archive-next/lib/archive-api.ts`) لها مالك واحد في كل دفعة.
- ملفات Setup المشتركة (`scripts/control-center.mjs` و`setup-config.mjs` وmanifest schemas و`compatibility.v1.json`) تُعالج في دفعة Native واحدة بعد تثبيت قرارات المنصة.
- V1-303B يسبق V1-303C..E، وV1-307A يسبق V1-307B..D. لا يغلق V1-501 أو V1-505 المحليان القبول الخارجي.
- لا تبدأ دفعة الاختبار والفحص والقبول (5) قبل إغلاق دفعات التطوير 2–4 المرتبطة بالمسارات المطلوب اختبارها.
- **قرار 2026-07-19:** لا بنود اختيارية في هذا الملف — صلابة المعمارية (V1-786/790) وتحققات القدرات الموسّعة (V1-X01..X04) إلزامية مانعة للإصدار مثل بقية البنود.

## الموجة 7 — مميزات الإصدار الأول (P8)

> رُقّيت هذه المميزات من Backlog بقرار المنتج في 2026-07-14، وهي مانعة للإصدار بموجب V1-406. أي بند يلمس API يحدّث `docs/api/archive-contract.openapi.json` في نفس الـcommit، وشرائح UI تُرفق لقطات 375/768/1280.

### P8A — إثراء السجل والبحث


### P8B — الرفع والاستيراد والتصدير


### P8C — التعاون والحضور والإشعارات


### P8D — السلامة والموثوقية


### P8E — الإنتاجية اليومية

### P8F — الأتمتة والتكامل والمراقبة

- [ ] **V1-762 تكامل Dropbox كامل** — OAuth 2.0 (ربط/فصل وتجديد token وتخزينه مشفّرًا)، اختيار المسار والمجلد، رفع وتنزيل متدفقان للملفات الكبيرة مع استئناف وإعادة محاولة، مزامنة/استيراد انتقائي، Webhooks موقعة مع idempotency وdead-letter، قيود quota/rate-limit وأخطاء قابلة للتعافي، واجهة إعداد وحالة عربية، واختبارات Laravel وPlaywright مع Dropbox mock ثم تحقق حي مشروط ضمن V1-X01. يعتمد على `spatie/dropbox-api` و`spatie/flysystem-dropbox` الحاليين، ولا يُفعّل افتراضيًا بلا credentials صريحة.

### P8G — صقل الواجهة والتجربة

- [ ] **V1-791 تدقيق لغوي شامل للعربية** — مراجعة وتصحيح كل نصوص الواجهة وعناوينها العربية (الصفحات، الأزرار، الرسائل، التلميحات، رسائل الأخطاء، إشعارات API) — إملاءً ونحوًا واتساق مصطلحات (مسرد موحد: سجل/مادة، مخزن/متجر، توصيف/بيانات وصفية...)، مع احترام RTL والأرقام والتواريخ؛ يشمل نصوص Laravel المعرَّبة أيضًا. — تقدم 2026-07-22: أُضيف المسرد الحاكم `docs/arabic-ui-glossary.md` بتمييز صريح بين «سجل» و«مادة» واعتماد «مخزن» و«بيانات وصفية»، وأُضيف `lib/arabic-terminology.ts` مع اختبار حارس حتمي يفحص نصوص TypeScript/TSX القانونية ويمنع «السيرفر» و«اللوج» في النص التشغيلي؛ 4 اختبارات موجّهة و466 اختبار Next وtypecheck وbuild (56 صفحة) خضراء. **متبقٍ:** التدقيق البشري الشامل لكل صفحات Next ونصوص Laravel واتساق التواريخ والأرقام واتجاه الأيقونات.
- [ ] **V1-793 دليل مستخدم احترافي محدث** — نظام دليل مستخدم متكامل داخل التطبيق (صفحة /help موسعة أو /guide): فصول حسب الدور (admin/editor/viewer) والمهام اليومية، لقطات/رسوم توضيحية، بحث داخل الدليل، وربط سياقي من كل صفحة (زر "كيف تعمل هذه الصفحة؟")؛ يُبنى من مصدر Markdown قابل للصيانة ويُحدَّث مع كل إصدار (يرتبط بحوار "ما الجديد").
- [ ] **V1-795 تدقيق تصميم UI/UX شامل قبل الإصدار الأول** — مراجعة تصميمية منهجية عبر كل الصفحات المصنفة، منفصلة عن الفحص التقني/الوصولية (V1-303E وV1-306B يغطيانه): **(1) الهوية البصرية العامة** — اتساق الألوان والطباعة والتباعد وهوية العلامة بدل مظهر عام/قوالب جاهزة غير مميزة؛ **(2) الاتساق بين الصفحات** — أنماط موحدة للأزرار والبطاقات والجداول والنماذج والحوارات بدل انحرافات محلية متراكمة؛ **(3) تجربة الموبايل واللمس** — مساحات لمس وتخطيطات 375/768 حقيقية لا امتدادًا لتخطيط سطح المكتب؛ **(4) سهولة الاستخدام وتدفق المهام** — وضوح اكتشاف الإجراءات الشائعة وتسلسلها المنطقي وعدم إغراق المستخدم بخيارات لا يحتاجها في سياقه. **المخرج:** قائمة مشاكل مرتّبة حسب الشدة (حرجة/عالية/متوسطة/منخفضة)، سطر واحد لكل ملاحظة (الموقع، المشكلة، الإصلاح المقترح)، مع لقطات عند 375/768/1280 للحالات غير الواضحة نصيًا. كل ملاحظة غير تافهة تتحول إلى بند V1 مستقل قبل التنفيذ (لا إصلاح عشوائي أثناء التدقيق نفسه)؛ الإصلاحات التافهة (سطر CSS واحد لا يغيّر السلوك) تُطبَّق مباشرة وتُذكر في المخرج. **دفعة تطوير لا اختبار** — مخرجه backlog تحسين يغذّي دفعة 2، وليس بوابة نجاح/فشل.

## صلابة المعمارية (إلزامية — قرار 2026-07-19)

> يبقى Laravel مصدر الحقيقة وواجهة API، وتبقى Next.js واجهة التشغيل. لا يُنقل منطق المجال إلى BFF ولا تُجزأ المنصة إلى microservices قبل دليل SLO/ملكية فريق مناسب. **كانت مصنّفة سابقًا "ما بعد الإصدار الأول، لا تمنع" — أصبحت مانعة للإصدار بقرار صريح 2026-07-19.**

- [ ] **V1-786 فصل مستوى تنفيذ الوسائط تدريجياً** — تثبيت عقد versioned لـ`media_jobs` ومخرجاته، ثم إتاحة عامل media مستقل قابل للتوسع CPU/GPU مع بقاء Laravel مالك الجدولة والحالة والتدقيق؛ لا تغيير للـAPI العامة دون عقد وترحيل متوافقين.
- [ ] **V1-790 حد وصول موحد لبيانات الأرشيف** — تصميم طبقة وصول محددة لـ`storage_rows` (هوية `[store, uid]`، read/write، معاملات، وفهارس JSON اللازمة) ثم ترحيل مسارات المجال تدريجيًا.

---

> **ما يلي هذا الفاصل هو دفعة الاختبار والفحص والقبول (الترتيب 5) — تُنفَّذ بعد إغلاق دفعتي التطوير أعلاه، لا بالتوازي معهما.**

## المنتج، الوصولية، والأداء

### V1-303 الوصولية والاستجابة

- [ ] **V1-303D لوحة المفاتيح وقارئ الشاشة** — tab order، focus traps، Escape، skip link، announcements وعينة فعلية لقارئ شاشة على onboarding/archive/record/upload/search/admin.

### V1-306 اللغة والمساعدة

- [ ] **V1-306B اتساق العربية وRTL** — تدقيق النصوص والمصطلحات والتواريخ والأرقام واتجاه الأيقونات عبر المسارات المصنفة، واختبار يمنع mojibake والنصوص التشغيلية الإنجليزية غير المعتمدة. — تقدم 2026-07-21: أُنجز شق "اختبار يمنع mojibake" فقط — `lib/mojibake-guard.ts` (دالة نقية `findMojibake`، تكتشف تشفير عربي UTF-8 أُسيء قراءته كـLatin-1) مع `lib/mojibake-guard.test.ts` يفحص كل ملفات `app/`/`components`/`lib` (باستثناء الاختبارات والملفات المولّدة) — صفر حالات mojibake حالياً في الكودبيس (لا نتائج إيجابية كاذبة). بقية البند (تدقيق المصطلحات/التواريخ/الأرقام/اتجاه الأيقونات يدويًا، ومنع النصوص الإنجليزية التشغيلية غير المعتمدة) لم يُنفَّذ بعد.

### V1-307 الأداء

- [ ] **V1-307B قياسات الواجهة** — LCP p75 ≤2.5s وCLS ≤0.1 وINP ≤200ms على جهاز baseline بعقد الموارد، للمسارات اليومية وعند نقاط العرض المطلوبة.
- [ ] **V1-307C قياسات API والرفع** — P95 البحث ≤1.5s، فتح السجل ≤1s، وبدء جلسة الرفع ≤2s دون زمن النقل، على Docker وNative بالأداة والبيانات نفسيهما.
- [ ] **V1-307D بوابة regression** — حفظ baseline ونتائج قابلة للمقارنة، وفشل CI/release rehearsal عند تجاوز budget دون استثناء إصدار موثق.

## القبول الخارجي للمنصات

> **قرار 2026-07-19:** يمكن تجربة القبول محليًا على جهاز التطوير الحالي (Windows 11 Pro) كخطوة عملية مبكرة، وليس بديلًا نهائيًا عن clean-host إن اقتضت البوابة النهائية (V1-505/V1-601) جهازًا نظيفًا مُصوَّرًا حديثًا بلا أدوات تطوير.

### Windows Native

- [ ] **V1-210D قبول Windows نظيف — تحقق خارجي** — Windows 10 و11: online/offline، local/intranet/public TLS، reboot، backup/restore، update/rollback، uninstall/reconnect؛ إرفاق logs/manifest/support bundle منزوعة الأسرار. **يمكن تنفيذ تمريرة أولى مباشرة على هذا الجهاز** (Windows 11 Pro حقيقي، لا محاكاة Docker/VM)؛ الإغلاق النهائي الموقّع لـGA قد يتطلب جهازًا نظيفًا مُصوَّرًا حديثًا بحسب صرامة البوابة عند الإصدار.

### Linux Native


## برنامج القبول التفاعلي متعدد المنصات

> التصميم الحاكم: [`docs/superpowers/specs/2026-07-19-v1-interactive-acceptance-matrix-design.md`](docs/superpowers/specs/2026-07-19-v1-interactive-acceptance-matrix-design.md). خطة تنفيذ الشريحة الأولى للوكيل: [`docs/agents/v1-acceptance-smoke-slice-implementation.md`](docs/agents/v1-acceptance-smoke-slice-implementation.md). يبدأ التنفيذ بالشريحة Docker smoke في V1-801..805؛ لا يبدأ provider جديد قبل خروج الأمر الواحد أخضر خلال أقل من 15 دقيقة. كل سيناريو يحمل معرّف `V1-IA-<المجال>-<الرقم>` وينتج manifest وأدلة منزوعة الأسرار.

### الشريحة الأولى — منسق Docker وSmoke

- [ ] **V1-801 عقد السيناريو والمنسق الأساسي** — registry versioned، tags (`smoke/daily/nightly/rc/ga/external`)، حالات حاسمة (`passed/failed/blocked-capability`)، اختيار بالمعرّف أو الوسم، وحساب ميزانية login/refresh قبل التشغيل؛ الأمر القانوني `node scripts/acceptance.mjs run --tag smoke`.
- [ ] **V1-802 مخزن الأدلة والفحص ضد التسرب** — summary وrun manifest وlogs/screenshots/traces خارج شجرة المصدر، إعادة استخدام sanitizer الحالي، وفشل التشغيل إذا بقي token/password/API key/credential URL/مسار مستخدم في أي artifact؛ صلاحيات مالك فقط حيث يدعم النظام.
- [ ] **V1-803 Docker provider آمن وقابل للتنظيف** — `prepare/install/start/exec/collect/reset/destroy` مع أهداف صريحة، منافذ ديناميكية، health/readiness، cleanup في `finally`، وإثبات عدم بقاء container/network/volume أنشأها run؛ لا يمس بيئة Docker لم يبدأها المنسق.
- [ ] **V1-804 خمسة سيناريوهات Smoke تفاعلية** — `V1-IA-PLAT-001` إقلاع، `V1-IA-ARCH-001` دخول→بحث→فتح سجل، `V1-IA-ADMIN-001` صحة admin، `V1-IA-ADMIN-002` backup+verify، و`V1-IA-MULTI-001` جلستا دورين متزامنتان؛ تسجيل دخول حي لكل context وزرع إقرارات الحوارات من ثوابت الكود.
- [ ] **V1-805 دورة التشخيص والإصلاح وإعادة الفاشل** — تصنيف product/platform/data/environment/flake، إعادة flake واحدة فقط، `--last-failed` ومعرّفات صريحة، حفظ المحاولتين، وربط الفشل الذي يتجاوز يوم عمل ببند V1 جديد؛ لا إغلاق بلا manifest أخضر.

### توسيع المنصات ودورة الحياة

- [ ] **V1-806 عقود providers والقدرات** — واجهة موحدة تضيف `snapshot` الإلزامي قبل السيناريو المدمر، capability discovery، وadapters لـWindows Native وLinux Native وHyper-V Windows/Linux وWSL2 وexternal evidence؛ WSL2 لا يُقبل بدل clean-host Linux.
- [ ] **V1-807 مصفوفة دورة حياة المنصة** — تثبيت أول وoffline وreboot وreconnect وupdate/rollback وuninstall/keep-data، وأعطال port/DNS/TLS/disk/service على Docker وWindows وLinux؛ clean-host فقط يغلق V1-210D/V1-211D/V1-505/V1-604.

### رحلات الأدوار والعمل اليومي

- [ ] **V1-808 رحلة مدير النظام الكاملة** — first-run والتخزين/DB/TLS والميزات، المستخدمون والأدوار، الصحة والطوابير والسجلات والتدقيق، backup/verify/preview/restore وRPO/RTO، الصيانة وحزمة الدعم، وgame-day DB/cache/worker/Reverb/network/disk/certificate مع تحقق التعافي وعدم فساد البيانات.
- [ ] **V1-809 رحلة موظف الأرشفة الكاملة** — إدخال مفرد/متعدد/مجلد/drop/URL/سجل بلا ملف، توصيف عربي وحقوق وعلاقات، الملاحظات والتعليقات، البحث بأنماطه والبحوث المحفوظة، المجموعات والمشاركة والتصدير، الحذف/undo/restore، ومصفوفة صلاحيات viewer/editor/admin.
- [ ] **V1-810 رحلة المونتاج والتجميع والتصدير** — مشروع من بحث/مجموعة، ترتيب وقص وانتقالات ومسودة، مراجعة زمنية وتعليقات وannotations، thumbnail/transcode/transcription والإلغاء وإعادة المحاولة، تصدير وفحص checksum، وتعافي موثق من فشل FFmpeg/worker/disk دون نجاح كاذب.
- [ ] **V1-811 تعدد المستخدمين والتزامن** — جلسات مستقلة admin/editor/viewer، حضور وأقفال وانتهاؤها، stale version وحل التعارض، مشاركة وتفويض مؤقت، إشعار واحد وروابط عميقة، field ACL والحقوق وعزل الملاحظات، وتنظيف آمن بعد انقطاع المستخدم.

### الأحمال والبوابات

- [ ] **V1-812 بيانات وأحمال قابلة لإعادة الإنتاج** — ربط V1-307A ببذرة وmanifest: 100 ألف سجل عربي و10 آلاف ملف وعينة 1GB، آلاف jobs وعمليات متزامنة، soak ومقاييس الموارد والطوابير وسلامة العد/checksum؛ الأداء مرجعي يوميًا ومانع فقط على جهاز RC baseline.
- [ ] **V1-813 جدولة Daily/Nightly وبوابات RC/GA** — smoke يومي، مصفوفة VM وحمل متوسط nightly، RC كامل online/offline، وGA من artifacts النهائية الموقعة بلا build جديد؛ `blocked-capability` مانع للمنصة المعلنة، والاستثناء يتطلب قرار Product وأثرًا وحلًا مؤقتًا وتاريخ انتهاء.
- [ ] **V1-814 لوحة وتقارير أدلة القبول** — تقرير موحد بشري وآلي يربط commit/version/digests/provider/resources/scenario attempts، مقارنة runs وbudgets، روابط الأدلة، export لحزمة Product/Security/Ops/Support، ومدخل حاسم لـV1-501..505 وV1-601.

## تحققات القدرات الموسّعة — إلزامية للإصدار الأول (قرار 2026-07-19)

> كانت هذه البنود مصنّفة "اختيارية، معطلة افتراضيًا، لا تمنع GA" — أصبحت إلزامية مانعة بقرار صريح 2026-07-19. الميزات الأساسية لكل قدرة **مبنية بالفعل** في الكود (`archive-laravel/app/Services/Odbc/*`، `WhisperTranscriber.php`، `EmbeddingService.php`+`EmbeddingsSync`، ودعم Dropbox/S3 في `filesystems.php`) — المتبقي هو تمكينها افتراضيًا (الدفعة 4) ثم التحقق الحي الفعلي (الدفعة 5)، لا بناء ميزات من الصفر.

- [ ] **V1-X01 التخزين الخارجي — تحقق حي إلزامي** — Dropbox/S3 أو المزود المعلن: اعتماد حي، read/write/delete، ملف كبير، انقطاع وإعادة محاولة، وعدم تسرب الاعتماد. مانع للإصدار الأول.
- [ ] **V1-X02 ODBC على Windows — تحقق حي إلزامي** — DSN/driver حي وبيانات اختبار (V1-102G مغلق مسبقًا)؛ read/write المسموح ورفض الجدول/الدور غير المسموح. يُفعَّل افتراضيًا لا كـfeature flag معطلة.
- [ ] **V1-X03 تفريغ عربي GPU — تحقق حي إلزامي** — عينة عربية معتمدة ومقاييس دقة/زمن وموارد على GPU مستهدف؛ نتيجة الدليل تحدد profile media/التفريغ المدعوم فعليًا في الإصدار.
- [ ] **V1-X04 AI/vision/embeddings — تحقق حي إلزامي** — مزود وفهرس/خط معالجة حي، حدود timeout/rate/cost، عزل البيانات وفشل آمن؛ القدرة مطلوبة ومفعّلة في الإصدار الأول لا اختيارية.

## RC وتجربة ميدانية وGA

- [ ] **V1-501 Alpha داخلي وgame-day** — فشل DB/cache/queue/worker/Reverb/network/disk/certificate على تثبيت Docker وNative؛ توثيق الكشف والتنبيه والتعافي وعدم فساد البيانات.
- [ ] **V1-502 نشر rc.1 ميداني — تحقق خارجي** — 3–5 بيئات تغطي Windows/Linux وDocker/Native وonline/offline، باستخدام Setup وartifacts الموقعة فقط.
- [ ] **V1-503 قياسات التشغيل — تحقق خارجي** — زمن التثبيت، نجاح onboarding لكل دور، budgets الأداء، RPO/RTO من DR drill، استهلاك الموارد ومعدل الأخطاء.
- [ ] **V1-504 triage pilot** — إغلاق كل P0/P1؛ كل P2 إما مغلق أو مؤجل بقرار Product يذكر الأثر والحل المؤقت والموعد.
- [ ] **V1-505 release rehearsal نظيف** — checkout/runner بلا cache يبني artifacts مرة واحدة، يتحقق من التواقيع، ثم يثبت ويختبر النسخة على clean hosts دون build عند العميل.
- [ ] **V1-601 Go/No-Go** — توقيع Product/Security/Operations/Support بعد إرفاق نتائج P5/P6 والـpilot وعدم وجود مانع مفتوح.
- [ ] **V1-602 إصدار `v1.0.0`** — tag من commit مجمد، بناء artifacts مرة واحدة، ومطابقة version/digest/signature/provenance.
- [ ] **V1-603 نشر الإصدار** — الصور وحزم Docker/Windows Native/Linux Native والدليل وملاحظات الإصدار وSBOM/checksums والتواقيع.
- [ ] **V1-604 اختبار التنزيل النهائي — تحقق خارجي** — تثبيت online وoffline من artifacts العامة نفسها، لا من workspace أو registry خاص.
- [ ] **V1-605 فتح التشغيل والدعم** — لوحات الصحة المحلية، سياسة الدعم والتصعيد، runbooks للنسخ/الاستعادة/التحديث/التراجع، وقناة استقبال العيوب.

## بوابات التنفيذ لكل مهمة مفتوحة

- ابدأ باختبار فاشل يثبت السلوك أو العقد، ثم أقل تنفيذ ينجحه، ثم refactor محدود.
- تحقق بأضيق اختبار أو typecheck/Laravel/Node متأثر أثناء العمل. بعد استقرار الدفعة شغّل `pnpm typecheck` و`pnpm test:next` أو `pnpm verify:laravel`، و`pnpm verify:api-contracts` عند لمس العقد، وbuild واحد للدفعة.
- دفعة Setup/Native تشغّل أيضًا `node --test scripts/control-center.test.mjs scripts/platform-contract.test.mjs` و`pnpm verify:infra`.
- دفعة الرحلات والأدوار والوصولية تشغّل `pnpm verify:laravel-next:live` وتحفظ screenshots/traces/reports.
- مهام clean-host لا تُغلق بمحاكاة محلية؛ يجب إرفاق platform/version/mode/source/manifest ونتيجة كل scenario ID من الجهاز المستهدف.
- بعد القبول: commit مستقل، ثم إضافة سجل موجز أو موسع في `ChangeLog.md` وإزالة البند من هذا الملف.
