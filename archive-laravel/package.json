{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node -e \"console.log('No Laravel frontend build. Use pnpm build:next from the repository root.')\"",
        "dev": "node -e \"console.log('No Laravel Vite dev server. Use pnpm dev from the repository root.')\""
    }
}
