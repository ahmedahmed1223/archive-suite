apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: archive-ingress
  namespace: archive
  annotations:
    # cert-manager will provision and renew a Let's Encrypt TLS certificate.
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "256m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    # SSE streams for /api/sync/events must not be buffered.
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    # WebSocket support — required for the /ws presence endpoint.
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - archive.example.com   # Replace with your actual domain
      secretName: archive-tls
  rules:
    - host: archive.example.com   # Replace with your actual domain
      http:
        paths:
          - path: /ws
            pathType: Prefix
            backend:
              service:
                name: server
                port:
                  number: 8787
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: server
                port:
                  number: 8787
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
