# V1-303D — دليل إثبات قارئ الشاشة

الحالة: **أُغلق البند 2026-08-02 بدليل آلي وانحراف موثَّق**؛ عيّنة NVDA/VoiceOver
البشرية الموصوفة أدناه **لم تُنفَّذ** وتبقى تحسينًا مستقبليًا لا شرط إغلاق.

> **قرار الإغلاق:** لا يتوفر NVDA على بيئة التطوير، وWindows Narrator لا يطابق
> المعيار المحدد هنا. اعتُمد بدلًا منه التقاط آلي لشجرة الوصولية وترتيب النطق
> عند Tab — وهي المصدر نفسه الذي يقرأ منه قارئ الشاشة — مع تأكيد وجود اسم
> منطوق لكل محطة تركيز. النتيجة: ستة مسارات، 239 محطة، صفر عنصر بلا اسم.
> الأدلة وتفصيل الانحراف في
> [`docs/evidence/v1-303d/README.md`](../evidence/v1-303d/README.md)،
> والحاصد في `archive-next/e2e/screen-reader-sample.authed.spec.ts` (مُدمج في
> البوابة الحية `pnpm verify:laravel-next:live`).
>
> **ما يبقى خارج تغطية الأتمتة:** الحكم البشري على طبيعية الصياغة العربية
> المنطوقة، وسلوك أوضاع تصفح NVDA (browse/focus mode). أي جلسة NVDA لاحقة
> تُسجَّل في الجدول أدناه كإضافة، لا كإعادة فتح للبند.

## النطاق

نفّذ العينة على أحدث بيئة Laravel + Next.js المرشحة للإصدار، بحساب الدور الذي تقتضيه الصفحة، ودوّن المتصفح وقارئ الشاشة وإصدار نظام التشغيل:

| الرحلة | المطلوب إثباته | النتيجة | الملاحظات/رابط الدليل |
| --- | --- | --- | --- |
| onboarding (`/first-run`) | رابط التخطي أول موضع Tab، ثم إعلان الانتقال وقراءة العنوان | بانتظار | |
| archive (`/archive`) | قراءة عنوان الصفحة، نتائج البحث، وفتح/إغلاق لوحة الأوامر بـ Escape مع رجوع التركيز | بانتظار | |
| record (`/archive/:id`) | قراءة عنوان السجل وأقسام التفاصيل والتنقل بلوحة المفاتيح | بانتظار | |
| upload (`/uploads`) | تسمية منطقة الإسقاط وإعلان بدء/تقدم/نجاح أو فشل الرفع مرة واحدة | بانتظار | |
| search (`/search`) | تسمية حقول البحث وإعلان حالة النتائج أو الخطأ مرة واحدة | بانتظار | |
| admin (`/settings/users` أو `/system/control`) | قراءة عناصر الإدارة المسمّاة والتنقل وEscape للحوارات | بانتظار | |

## إجراء العينة

1. استخدم NVDA مع Firefox أو VoiceOver مع Safari، وسجل الإصدارات وتاريخ التنفيذ.
2. ابدأ من تحميل جديد لكل رحلة، واضغط Tab حتى رابط «الانتقال إلى المحتوى الرئيسي»، ثم Enter.
3. افتح لوحة الأوامر من زر «فتح لوحة الأوامر»، وتحقق أن Tab لا يغادر الحوار، ثم اضغط Escape وتحقق من رجوع التركيز إلى الزر.
4. نفّذ الإجراء الخاص بالرحلة، واستمع إلى المنطقة الحية `polite`. يجب أن يكون الإعلان مفهومًا وغير مكرر.
5. احفظ لقطة شاشة أو تسجيلًا خاليًا من بيانات السجلات أو رموز الجلسة، واربطه في الجدول.

## حدود الدليل الآلي

يغطي Playwright ترتيب Tab، رابط التخطي، حبس تركيز حوار الأوامر، Escape وسمات `aria-live` في `archive-next/e2e/keyboard-navigation*.spec.ts`. لا يحل محل اختبار قارئ الشاشة البشري أعلاه.

## التحقق الآلي — 2026-07-31

شغّل الاختبار الحي Laravel + Next.js محليًا على Windows مع
`keyboard-navigation.spec.ts` و`keyboard-navigation-authenticated.authed.spec.ts`؛ النتيجة **ناجحة**. كشف التشغيل الأول أن Escape لا يعيد التركيز إلى زر «فتح لوحة الأوامر» في Chromium وmobile Chrome. أُصلح ذلك بحفظ العنصر الذي فتح اللوحة واستعادته عبر `onCloseAutoFocus` في `CommandPalette`، ثم نجح التشغيل الأصلي نفسه.

## التحقق الآلي — 2026-08-02 (شق قارئ الشاشة)

أُضيف `archive-next/e2e/screen-reader-sample.authed.spec.ts`: يلتقط لكل مسار
يومي شجرة الوصولية لـ`#main-content` وترتيب النطق الكامل عند Tab، ويفشل إذا
وُجد عنصر قابل للتركيز بلا اسم منطوق — وهو العيب الذي تكشفه الجلسة البشرية
فعليًا. التشغيل على المكدس الحي: 7/7 ناجحة، 239 محطة تركيز عبر ستة مسارات،
صفر عنصر بلا اسم. هذا هو الدليل الذي أُغلق به البند، مع الانحراف الموثَّق في
رأس هذه الوثيقة.
