# تسليم متابعة الإصدار 2 — 2026-09-13

## الغرض

هذه الوثيقة هي نقطة البدء لوكيل يكمل تطوير الإصدار 2 من **Archive Suite**. لا تعني أن الإصدار منشور أو جاهز للإطلاق؛ فهي تسجل الحالة المثبتة فقط وتمنع تكرار العمل أو تجاوز حدود النطاق.

## المسار المعتمد والحدود

- واجهة المنتج المعتمدة: `archive-next/`.
- الخدمة المعتمدة: `archive-laravel/`.
- عقد الواجهة العامة: `docs/api/archive-contract.openapi.json`.
- لا تضف مزايا جديدة إلى `archive-app/` أو `archive-server/`؛ هما مرجعان قديمان فقط.
- لا تنفذ `push` أو نشرًا أو إصدارًا أو تعديلًا للصلاحيات دون موافقة صريحة من المالك.
- احفظ أي تغييرات موجودة وغير مرتبطة بالمهمة، وبالأخص `scripts/fetch-native-release-inputs.mjs` وملفات `.stitch/**` غير المتتبعة.

## ما أُنجز ومكانه

| الالتزام | النتيجة |
| --- | --- |
| `5c152c81` | نقطة وصول محمية تقرأ محتوى المشتق الإعلامي الحالي فقط، دون كشف مفتاح التخزين. |
| `11e12e09` | عميل Next.js يجلب المحتوى المحمي كبايتات مع تجديد الجلسة عند الحاجة. |
| `43056c9f` | ملخص إعلامي مجمّع للصفحة في واجهة السجلات، مع العقد والأنواع المولّدة. |
| `fafbd417` | بطاقة أرشيف تشغيلية تعرض الملصق الحالي، المدة، حالة النسخة البديلة، وحالة الفحص. |
| `873c93db` | إصلاح مسار البحث الفعلي: يضيف ملخص الوسيط إلى نتائج البحث بعد التقسيم إلى صفحات، واختبار حي يمنع طلب المشتق القديم. |
| `0c6682d9` | استبدال عنصر «مهام المادة» المؤجل في استوديو الوسائط بلوحة مهام حقيقية مرتبطة بالسجل، تشمل حالات التحميل والخطأ والفراغ وترتيب العمل غير المكتمل أولًا. |

## أدلة التحقق الحالية

- اختبارات Laravel المستهدفة لمسار البحث: **18 اختبارًا، 143 تحققًا** ناجحًا.
- اختبارات مكوّن الملصق والبطاقة: **13 اختبارًا** ناجحًا.
- `pnpm typecheck`: ناجح.
- `pnpm --filter @archive/next run build`: ناجح.
- `pnpm verify:laravel-next:live`: ناجح، ويغطي بطاقة فيديو مسجلة مع مشتق حالي وآخر قديم لا يجب طلبه.
- `pnpm verify`: ناجح في 2026-09-13 بصلاحية Docker (`exit 0`): 1230 اختبار واجهة، بناء Next.js، تدقيق النظافة، واختبارات Laravel. يبقى النشر قرارًا منفصلًا لا يترتب على نجاح البوابة.
- اختبارات لوحة مهام السجل واستوديو الوسائط: **5 اختبارات** ناجحة؛ و`pnpm typecheck` وبناء Next.js ناجحان بعد الالتزام `0c6682d9`.

## حالة Stitch وتتبع قرارات التصميم

- مشروع Stitch الصحيح هو `projects/12079300562270009833`، ونظام التصميم النشط فيه هو `Operational Excellence` (`assets/769859ba548141418e0b4955e3a21f00`). سجّل المشروع في Stitch **23 شاشة** عند آخر فحص؛ أما `.stitch/metadata.json` فيسجل 15 شاشة فقط، ولذلك فهو فهرس محلي قديم لا مصدر عدّ نهائيًا.
- تم التحقق فعليًا في 2026-09-13 من `list_projects` و`list_screens` عبر اعتماد Google المؤقت للحساب المهيأ؛ يعيد Stitch مشروع Archive Suite وشاشاته، ومنها تسجيل الدخول والاستقبال وسجل المادة والبحث والحوكمة والتعاون وإدارة النظام. لا يظهر رمز الوصول في الطرفية أو ملفات المشروع.
- خلل الإعداد المحدد: `init` يلتقط المسار `gcloud` بلا امتداد في Windows، فيفشل برسالة `Could not determine gcloud version` رغم أن `gcloud.cmd` يعمل. كما أن نسخة الخادم تقرأ `GOOGLE_CLOUD_PROJECT` لمسار OAuth، لا `STITCH_PROJECT_ID`. لا تحفظ رمز OAuth في إعداد Codex أو غلاف دائم؛ فهو اعتماد قصير العمر ويحتاج إدارة أسرار صريحة إذا تقرر تشغيله تلقائيًا لاحقًا.
- تجربة `generate_screen_from_text` لشاشة استوديو الوسائط وفحص الجودة لم تُنشئ شاشة ولم تُرجع معرّفًا أو خطأً، بينما بقي عدد الشاشات 23. لا تعِد هذا الطلب تلقائيًا، وفق تعليمات Stitch. شخّص هذا العطل من جانب موصل التوليد أو استخدم أدوات Stitch المحقونة في جلسة Codex بعد إصلاح اعتماده؛ لا تسجل الشاشة كمنجزة ولا تنسخ ناتجًا غير موجود.
- عند عودة التوليد، ابدأ بفجوات العائلات الوظيفية: استوديو الفيديو والفحص، سير العمل والمهام، التحليلات والتقارير، الاستكشاف الزمني والمكاني والعلاقي، وإدارة المحتوى المرجعي والحقوق. كل مطالبة تكون بنيوية عربية RTL وتلتزم بنظام التصميم، ثم تُراجع النتيجة مقابل عقود Laravel وNext.js قبل التنفيذ.

## المهمة التالية بالترتيب

1. شغّل `pnpm verify` من جذر المستودع. إن فشل، شخّص السبب بأصغر اختبار يعيد المشكلة، ولا تعالج فشلًا غير متعلق بهذه الشريحة داخل نفس الالتزام.
2. راجع صفحات الإصدار 2 مقابل احتياجات أرشفة الفيديو الواقعية: الاستيعاب، سجل المادة، التوصيف، المشتقات، الفحص، البحث، المهام، الصلاحيات، والاحتفاظ. وثّق الفجوات كمهام مستقلة قبل إضافة سلوك جديد.
3. لكل تعديل لعقد عام، حدّث وثيقة OpenAPI وتنفيذ Laravel وعميل Next.js وفحوص العقد في الالتزام نفسه.
4. اختبر الواجهة ببيانات تجريبية عبر المسار الموثق أو بالاختبار الحي؛ لا تنشئ حسابات أو بيانات إنتاجية لمجرد فحص بصري.
5. قبل اقتراح نشر، نفّذ بوابة التحقق الكاملة، راجع التغييرات غير التابعة للمهمة، ثم اطلب موافقة صريحة مستقلة للنشر.

## ملاحظات التنفيذ المهمة

- صفحة `/archive` تستخدم `api.search`، لا `api.records`. لذلك يجب أن يبقى `SearchController` محافظًا على `mediaSummary` بعد تحديد الصفحة، في البحث النصي والدلالي معًا.
- يتحقق الملصق من حالة المشتق ومن تطابق `version_token` مع المادة. لا تعرض ملصقًا قديمًا كبديل صامت.
- اختبار التكامل يهيئ سجلاته في `archive-laravel/database/seeders/NextIntegrationSeeder.php`؛ المعرّف الحالي للصورة المصغرة هو `44444444-4444-4444-8444-444444444444` والقديم هو `55555555-5555-4555-8555-555555555555`.
- التخطيط التفصيلي المحلي لهذه الشريحة موجود في `docs/superpowers/plans/2026-09-13-v2-video-gallery-media-summary.md`، لكنه مجلد متجاهل من Git؛ استخدم هذه الوثيقة المتتبعة كسجل التسليم القابل للمشاركة.

## شرط الإنجاز لهذه الدفعة

لا تضع علامة «مكتمل» على الإصدار 2. تنتهي هذه الدفعة فقط عندما تنجح بوابة `pnpm verify` أو يُسجَّل سبب فشل واضح ومحدد، مع إبقاء النشر خارج النطاق حتى يوافق المالك صراحة.
