# V3-GATE-001: مصفوفة القدرة والدور وحالة النشر

**تاريخ المراجعة:** 2026-08-15
**نطاق الدليل:** شجرة الإصدار `codex/v1.3` فقط. لا يثبت هذا المستند أن مثيلًا
حيًا قد نُشر؛ فهو يميز عمدًا بين القدرة الموجودة في الشفرة وحالة مثيل النشر.

## دلالات الحالة ومالك الإعداد

| المصطلح | المعنى |
| --- | --- |
| متاحة في الشفرة | القدرة مسجلة في المسار المرجعي، لكن لم يُثبت تشغيلها في هذه البيئة. |
| مشروطة بالنشر | لا تتاح إلا بعد ضبط متغيرات البيئة أو خدمة خارجية. مالكها مسؤول النشر، لا الواجهة. |
| محجوبة في البيئة | تعذر التحقق منها الآن بسبب حاجب موثق في دليل MCP. |
| قبول جزئي | تحقق نقل أو جزء محدد فقط؛ تسجل الأجزاء المحجوبة ومالكها صراحة. |
| مؤجلة في 1.3 | ليست قدرة قابلة للتشغيل في نطاق الإصدار، ولا تضيف هذه المهمة إعدادًا أو واجهة لها. |

«مالك الإعداد» هو الجهة التي تملك مصدر الحقيقة للتشغيل: **الإصدار** لإعدادات
مضمنة في الشفرة، **مسؤول النشر** لمتغيرات البيئة والبنية، و**مدير الأرشيف**
للتشغيل المصرح به داخل المنتج. لا يعني ذلك أن المستخدم يملك سرًا أو يمكنه
تعديل إعدادات النشر من الواجهة.

## الأدوار المعتمدة

مصدر الأدوار هو `users.role` في Laravel: `admin` و`editor` و`viewer`
(`archive-laravel/app/Http/Controllers/Api/V1/UsersController.php`). وتعرض
واجهة Next.js مصفوفة الصلاحيات الصريحة في
`archive-next/lib/role-capabilities.ts`.

| القدرة في الواجهة | مدير الأرشيف | المحرر | المشاهد | مالك الإعداد | الحالة |
| --- | --- | --- | --- | --- | --- |
| إنشاء السجلات وتحريرها وحذفها جماعيًا | نعم | نعم | لا | إصدار التطبيق؛ تفويض الخادم هو الحكم النهائي | متاحة في الشفرة |
| إدارة المستخدمين | نعم | لا | لا | مدير الأرشيف | متاحة في الشفرة |
| التحكم بالنظام: مسح الذاكرة المؤقتة أو تشغيل النسخ الاحتياطي من مسار التحكم | نعم | لا | لا | مسؤول النشر لـ `SYSTEM_CONTROL_ENABLED`، ثم مدير الأرشيف | مشروطة بالنشر؛ المتغير معطل افتراضيًا |
| النسخ الاحتياطية: القائمة والتشغيل والمعاينة والاستعادة والتحقق وتمرين التعافي | نعم | لا | لا | مسؤول النشر لمسار `ARCHIVE_BACKUP_PATH` والتشفير والاحتفاظ؛ مدير الأرشيف للتشغيل | متاحة في الشفرة؛ لا يحرسها `SYSTEM_CONTROL_ENABLED` |
| السلة: الاستعادة | نعم | نعم | لا | مسؤول النشر لـ `TRASH_RETENTION_DAYS`؛ تفويض الخادم للتشغيل | متاحة في الشفرة؛ لا يحرسها `SYSTEM_CONTROL_ENABLED` |
| السلة: التطهير النهائي | نعم | لا | لا | مسؤول النشر لـ `TRASH_RETENTION_DAYS`؛ مدير الأرشيف للتشغيل | متاحة في الشفرة؛ لا يحرسها `SYSTEM_CONTROL_ENABLED` |
| قواعد الأتمتة، الحقوق، الإدخال، المجموعات، الوسوم، المفردات، القوالب، التفويضات، والمشاركات | نعم | نعم | لا | إصدار التطبيق؛ صلاحيات الخادم | متاحة في الشفرة |
| MCP: أدوات القراءة وطلب مراجعة بشرية | بحسب أداة MCP؛ طلب المراجعة للمدير والمحرر | بحسب أداة MCP؛ طلب المراجعة للمدير والمحرر | القراءة بحسب تفويض API؛ لا طلب مراجعة | مسؤول النشر لصورة Docker ونقطة HTTP وOAuth؛ `mcp.review_request_roles` لطلب المراجعة | قبول جزئي: `stdio` مقبول؛ HTTP المنشور والعميل الخارجي محجوبان |

لا تختزل هذه المصفوفة حواجز Laravel الدقيقة في حواجز الواجهة: مسارات الخادم
واختبارات `RoleMatrixApiTest` هي الحكم التنفيذي عند وجود تعارض.

## القدرات وحالة النشر

| القدرة | المصدر القابل لإعادة الفحص | الدور أو القيد | حالة النشر في هذه المراجعة | مالك الإعداد |
| --- | --- | --- | --- | --- |
| واجهة الأرشيف وواجهة API | `archive-next/` و`archive-laravel/` و`infra/docker-compose.laravel-next.yml` | أدوار المستخدم الثلاثة | متاحة في الشفرة؛ لم يُشغّل مثيل محلي | مسؤول النشر |
| الإدخال والتحميل المتقطع | `archive-laravel/config/ingest.php` | المدير والمحرر للعمليات الإدارية | مشروطة بقرص الإدخال و`INGEST_*` | مسؤول النشر |
| معالجة الوسائط والنص | `archive-laravel/config/media.php` | يتطلب تفويض مسارات الوسائط | مشروطة بـ `MEDIA_PROCESSOR=real` والعامل وFFmpeg/Whisper؛ الافتراضي المحلي `fake` | مسؤول النشر |
| OCR | `infra/ocr-service/` وملف Compose | خدمة داخلية فقط | مشروطة بملف Compose `media` وبـ `OCR_SERVICE_URL` | مسؤول النشر |
| التحكم المضيف: مسح الذاكرة المؤقتة وتشغيل النسخ من مسار التحكم | `SystemControlService.php` و`config/archive.php` | مدير الأرشيف فقط | مشروط بـ `SYSTEM_CONTROL_ENABLED=false` افتراضيًا | مسؤول النشر ثم مدير الأرشيف |
| النسخ الاحتياطي: تشغيل واستعادة وتحقق وتمرين التعافي | `BackupsController.php` و`BackupService.php` و`config/archive.php` | مدير الأرشيف فقط | متاحة في الشفرة؛ تحتاج مسار نسخ قابلًا للكتابة، و`ARCHIVE_BACKUP_PATH`/إعداد التشفير عند تخصيصها؛ لا تتوقف على `SYSTEM_CONTROL_ENABLED` | مسؤول النشر لمسار التخزين والتشفير؛ مدير الأرشيف للعمليات |
| سلة السجلات: استعادة وتطهير واحتفاظ | `TrashController.php` و`config/archive.php` | الاستعادة للمدير والمحرر؛ التطهير للمدير فقط | متاحة في الشفرة؛ الاحتفاظ تحكمه `TRASH_RETENTION_DAYS`؛ لا تتوقف على `SYSTEM_CONTROL_ENABLED` | مسؤول النشر للاحتفاظ؛ تفويض الخادم للعمليات |
| ODBC وبيانات البث MOS/MXF | `archive-laravel/config/archive.php` و`.env.example` | ODBC إداري؛ البث بحسب المسار | مشروطة بـ `ARCHIVE_FEATURE_*` ونقاط الاتصال/DSN؛ لا تدّعي المصفوفة وجود تكامل خارجي | مسؤول النشر |
| البحث الدلالي القائم على embeddings الموجود مسبقًا | `archive-laravel/config/embeddings.php` | يخضع لبحث السجلات وصلاحياته | معطل افتراضيًا ويحتاج PostgreSQL/`vector` ومزودًا؛ لا يُعد توسعًا جديدًا في 1.3 | مسؤول النشر |
| خادم MCP: بحث السجلات وقراءتها والأنواع وحالة النظام وطلب مراجعة بشرية | `archive-laravel/routes/ai.php` و`app/Mcp/Servers/ArchiveMcpServer.php` | HTTP محمي بـ OAuth؛ الأداة الكتابية تنشئ مسودة فقط | قبول جزئي: `stdio` مقبول عبر صورة Docker المعتمدة؛ HTTP المنشور محجوب لغياب عنوان اختبار ورمز OAuth قصير العمر، والعميل الخارجي محجوب لغياب عميل مثبت | مسؤول النشر لصورة Docker ونقطة HTTP وOAuth؛ الفريق لعميل MCP الخارجي؛ مدير الأرشيف لإدارة المستخدمين |
| توسعات AI الجديدة: سؤال الأرشيف، الترتيب الدلالي، تعدد الوسائط، اقتراحات سير العمل، وسياسة مزودين متقدمة | هذا المستند، قسم «سجل تأجيل توسعات AI» | لا دور مستخدم؛ خارج نطاق 1.3 | **مؤجلة في 1.3** | مالك الإصدار؛ لا إعداد تشغيل ولا واجهة في هذه المهمة |

## سجل تأجيل توسعات AI

هذا السجل جزء من الالتزام الحالي، وهو مرجع نطاق ذاتي التتبع لـ V3-GATE-001:

| المعرف | التوسع المؤجل | حالة 1.3 | مالك قرار النطاق |
| --- | --- | --- | --- |
| D3-AI-01 | سؤال الأرشيف | مؤجل؛ لا إعداد ولا واجهة جديدة | مالك الإصدار |
| D3-AI-02 | الترتيب الدلالي | مؤجل؛ لا إعداد ولا واجهة جديدة | مالك الإصدار |
| D3-AI-03 | تعدد الوسائط | مؤجل؛ لا إعداد ولا واجهة جديدة | مالك الإصدار |
| D3-AI-04 | اقتراحات سير العمل | مؤجل؛ لا إعداد ولا واجهة جديدة | مالك الإصدار |
| D3-AI-05 | سياسة مزودين متقدمة | مؤجل؛ لا إعداد ولا واجهة جديدة | مالك الإصدار |

## حدود قرار الإصدار

- لا تضيف V3-GATE-001 متغير بيئة أو نقطة API أو صفحة لتوسعات AI المؤجلة.
- لا يجوز وصف أي صف «متحقق في الإنتاج» قبل تشغيل قبول حي مستقل بمثيل وبيانات
  اعتماد يقدمهما مسؤول النشر.
- قبول MCP مقيد حاليًا بحاجب البيئة المسجل في
  `docs/v3-gate-001-mcp-acceptance.ar.md`؛ لذلك لا يفتح هذا
  المستند الموجة التالية بوصفه نجاح اتصال.
