{
  "Servers": {
    "1": {
      "Name": "Archive Postgres",
      "Group": "Archive Suite",
      "Host": "postgres",
      "Port": 5432,
      "MaintenanceDB": "archive",
      "Username": "archive",
      "SSLMode": "prefer",
      "Comment": "Bundled Postgres connection. Authentication uses the generated pgpass file from POSTGRES_PASSWORD."
    }
  }
}
